/** Generated from OneNote page XML + embedded images; polished for site */
export const NOTE_COLUMNS = [
  {
    "id": "java-web",
    "title": "Java Web",
    "subtitle": "HTML / HTTP / Session / Spring Boot",
    "summary": "从 WEB 标准、会话机制到 Spring Boot 自动配置，按模块整理并附带原笔记配图。",
    "topics": [
      {
        "id": "web-html-css",
        "title": "WEB 标准与 HTML / CSS",
        "summary": "HTML 结构、CSS 样式、盒子模型、表单与前端基础。",
        "html": "<p>Java webWEB 标准</p>\n<h4>Css 层叠样式表 用于控制页面的样式（表现）</h4>\n<p>Css 层叠样式表 用于控制页面的样式（表现）&lt;html&gt;</p>\n<pre class=\"note-code\">&lt;head&gt;</pre>\n<p>网页头部 用来存放给浏览器看的信息&lt;title&gt; &lt;/title&gt;&lt;/head&gt;</p>\n<pre class=\"note-code\">&lt;body&gt;</pre>\n<p>网页主题 用来存放给用户看的信息 比如文字 图片 视频&lt;h1&gt;&lt;/h1&gt;</p>\n<pre class=\"note-code\">&lt;body&gt;</pre>\n<pre class=\"note-code\">&lt;/html&gt;</pre>\n<figure class=\"note-figure\"><img src=\"/notes-media/java-web-002.png\" alt=\"WEB 标准与 HTML / CSS 笔记配图\" loading=\"lazy\" /></figure>\n<figure class=\"note-figure\"><img src=\"/notes-media/java-web-003.png\" alt=\"WEB 标准与 HTML / CSS 笔记配图\" loading=\"lazy\" /></figure>\n<p>标题标签 从 h1 到 h6 字体越来越细笑傲</p>\n<h4>a 标签是超链接标签</h4>\n<p>href 是链接地址 target 打开方式 _blank 新窗口打开 _self 在自身窗口打开</p>\n<figure class=\"note-figure\"><img src=\"/notes-media/java-web-004.png\" alt=\"WEB 标准与 HTML / CSS 笔记配图\" loading=\"lazy\" /></figure>\n<pre class=\"note-code\">&lt; span style =&quot; color : gray ;&quot; &gt; 2024 年 5 月 1 日 &lt;/ span &gt;优点 ：优先级最高，能覆盖其他样式适合临时修改单个元素缺点 ：</pre>\n<h4>代码冗余，无法复用</h4>\n<p>结构和样式混在一起，维护困难不利于团队协作第二种方式优点 ：样式集中管理，结构与样式初步分离适合单页面小项目，不用额外文件缺点 ：</p>\n<h4>只能作用于当前页面，无法跨页面复用</h4>\n<p>代码量大时，HTML 文件会变得臃肿第三种方式优点 ：结构与样式完全分离，代码最清晰</p>\n<h4>可跨页面复用，适合大型项目</h4>\n<p>浏览器会缓存 CSS 文件，提升加载速度缺点 ：</p>\n<h4>id 选择器优先级最高 其次是类选择器 最后是元素选择器</h4>\n<p>id 选择器优先级最高 其次是类选择器 最后是元素选择器/* 去除超链接下划线 */</p>\n<h4>a {</h4>\n<p>text-decoration : none ;}</p>\n<figure class=\"note-figure\"><img src=\"/notes-media/java-web-007.png\" alt=\"WEB 标准与 HTML / CSS 笔记配图\" loading=\"lazy\" /></figure>\n<figure class=\"note-figure\"><img src=\"/notes-media/java-web-008.png\" alt=\"WEB 标准与 HTML / CSS 笔记配图\" loading=\"lazy\" /></figure>\n<pre class=\"note-code\">&lt;!-- 正文 --&gt;</pre>\n<pre class=\"note-code\">&lt;!-- 换行的标签是 br --&gt;</pre>\n<pre class=\"note-code\">&lt; br &gt;</pre>\n<pre class=\"note-code\">&lt;!-- 定义一个视频 引入 video/1.mp4 --&gt;</pre>\n<pre class=\"note-code\">&lt; video src = &quot;video/1.mp4&quot; width = &quot;500px&quot; height = &quot;300px&quot; controls autoplay &gt;&lt;/ video &gt;&lt;!-- video 标签属性：</pre>\n<h4>height ：视频高度</h4>\n<p>单位： px 像素 % 百分比（相对于父元素的百分比）</p>\n<p>建议高度和宽度设置一个 因为会等比缩放controls ：显示控制栏autoplay ：自动播放</p>\n<h4>muted ：静音播放</h4>\n<p>poster ：视频封面src ：视频源文件--&gt;</p>\n<pre class=\"note-code\">&lt;!-- 如果引入音频需要使用 audio 标签 --&gt;</pre>\n<pre class=\"note-code\">&lt; br &gt;</pre>\n<pre class=\"note-code\">&lt; audio src = &quot;audio/1.mp3&quot; controls autoplay &gt;&lt;/ audio &gt;</pre>\n<pre class=\"note-code\">&lt;!-- 每段开头使用一个缩进 --&gt;</pre>\n<pre class=\"note-code\">&lt; p &gt; asdjfklsdjfklsdjfklsdjfklsdjfklsdjf &lt;/ p &gt;</pre>\n<pre class=\"note-code\">&lt; p &gt; dasdafjskdnoidsjfsojsiodjpwodno45 &lt;/ p &gt;</pre>\n<pre class=\"note-code\">&lt; p &gt; asdjaofdslfkmsldfmsldkfjsdlkfjslkdjf &lt;/ p &gt;</pre>\n<h4>src ：图片源文件</h4>\n<p>1 相对路径：分为文件路径和文件名 例如 index.html/img/1.jpg文件路径：当前文件所在目录</p>\n<h4>文件名：当前文件名</h4>\n<p>2 绝对路径： http://www.baidu.com</p>\n<p>3 网络路径： http://www.baidu.com</p>\n<p>4 数据路径： data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAAA</p>\n<h4>width ：图片宽度</h4>\n<p>height ：图片高度</p>\n<h4>title ：图片提示</h4>\n<p>srcset ：图片源文件</p>\n<h4>sizes ：图片大小</h4>\n<p>loading ：图片加载方式longdesc ：图片长描述--&gt;</p>\n<pre class=\"note-code\">&lt; img src = &quot;img/1.jpg&quot; alt = &quot; 图片加载失败 &quot; width = &quot;500px&quot; height = &quot;300px&quot; &gt;&lt;/ img &gt;&lt;p&gt; 就是分段标签</pre>\n<h4>p {</h4>\n<p>/* 设置行高 首行缩进 2 个空格 */</p>\n<p>line-height : 1.5 ;</p>\n<p>text-indent : 2em ;}</p>\n<figure class=\"note-figure\"><img src=\"/notes-media/java-web-009.png\" alt=\"WEB 标准与 HTML / CSS 笔记配图\" loading=\"lazy\" /></figure>\n<figure class=\"note-figure\"><img src=\"/notes-media/java-web-010.png\" alt=\"WEB 标准与 HTML / CSS 笔记配图\" loading=\"lazy\" /></figure>\n<h4>盒子模型</h4>\n<p>页面中所有的元素标签都可以看作是一个盒子 盒子将页面中的元素包含在一个矩形区域内 通过盒子的视角更方便的进行页面布局</p>\n<p>内容区域 (content) 内边距区域 (padding) 边框区域 (border) 外边距区域 (margin)</p>\n<figure class=\"note-figure\"><img src=\"/notes-media/java-web-011.png\" alt=\"WEB 标准与 HTML / CSS 笔记配图\" loading=\"lazy\" /></figure>\n<figure class=\"note-figure\"><img src=\"/notes-media/java-web-012.png\" alt=\"WEB 标准与 HTML / CSS 笔记配图\" loading=\"lazy\" /></figure>\n<figure class=\"note-figure\"><img src=\"/notes-media/java-web-013.png\" alt=\"WEB 标准与 HTML / CSS 笔记配图\" loading=\"lazy\" /></figure>\n<figure class=\"note-figure\"><img src=\"/notes-media/java-web-014.png\" alt=\"WEB 标准与 HTML / CSS 笔记配图\" loading=\"lazy\" /></figure>\n<h4>Height 默认是内容展示区域的高度</h4>\n<p>页面原型 在应用程序开发初期 由产品经理制作的一个早期项目模型 用于 展示页面的基本布局 功能和交互设计 帮助设计师开发者等更好的理解flex 弹性布局</p>\n<figure class=\"note-figure\"><img src=\"/notes-media/java-web-015.png\" alt=\"WEB 标准与 HTML / CSS 笔记配图\" loading=\"lazy\" /></figure>\n<p>表单：在网页中主要负责数据采集功能 如 注册登录等数据采集标签： &lt;form&gt;</p>\n<figure class=\"note-figure\"><img src=\"/notes-media/java-web-016.png\" alt=\"WEB 标准与 HTML / CSS 笔记配图\" loading=\"lazy\" /></figure>\n<figure class=\"note-figure\"><img src=\"/notes-media/java-web-017.png\" alt=\"WEB 标准与 HTML / CSS 笔记配图\" loading=\"lazy\" /></figure>\n<h4>Get 默认 表单数据会出现在 url 后面</h4>\n<p>特点 如果表单中包含了隐私数据 get 方式不安全 不推荐</p>\n<p>在在浏览器中 get 请求的大小是有限制的 不适合提交大数据量的表单</p>\n<p>Post 表单数据会在消息体 / 请求体中提交到服务器的</p>\n<h4>特点 1 安全 2 请求大小没有限制</h4>\n<p>注意 表单项想要能够采集数据 必须得设置 name 属性 表示当前表单项的名字</p>\n<pre class=\"note-code\">&lt; form action = &quot;/img/&quot; method = &quot;get&quot; &gt; 姓名 : &lt; input type = &quot;text&quot; name = &quot;username&quot; &gt;</pre>\n<p>年龄： &lt; input type = &quot;number&quot; name = &quot;userage&quot; &gt;</p>\n<pre class=\"note-code\">&lt; input type = &quot;submit&quot; value = &quot; 提交 &quot; &gt;&lt;/ L &gt;</pre>\n<figure class=\"note-figure\"><img src=\"/notes-media/java-web-018.png\" alt=\"WEB 标准与 HTML / CSS 笔记配图\" loading=\"lazy\" /></figure>\n<figure class=\"note-figure\"><img src=\"/notes-media/java-web-019.png\" alt=\"WEB 标准与 HTML / CSS 笔记配图\" loading=\"lazy\" /></figure>\n<p>Javascript 是一门跨平台 面向对象的脚本语言 是用来控制行为 实现页面的交互效果</p>\n<figure class=\"note-figure\"><img src=\"/notes-media/java-web-020.png\" alt=\"WEB 标准与 HTML / CSS 笔记配图\" loading=\"lazy\" /></figure>\n<figure class=\"note-figure\"><img src=\"/notes-media/java-web-021.png\" alt=\"WEB 标准与 HTML / CSS 笔记配图\" loading=\"lazy\" /></figure>\n<figure class=\"note-figure\"><img src=\"/notes-media/java-web-022.png\" alt=\"WEB 标准与 HTML / CSS 笔记配图\" loading=\"lazy\" /></figure>\n<p>声明变量 let( 弱类型语言 变量可以存放不同类型的值 )</p>\n<p>声明常量 const 一旦使用就无法更改</p>\n<h4>变量名的规则</h4>\n<p>1 只能用 字母 数字 下划线 美元符号组成 且数字不能开头</p>\n<p>2 变量名严格区分大小写 不能使用关键字</p>\n<p>旧版本的声明常量使用 var 但是不严谨</p>\n<p>console.log() 输出到控制台</p>\n<figure class=\"note-figure\"><img src=\"/notes-media/java-web-023.png\" alt=\"WEB 标准与 HTML / CSS 笔记配图\" loading=\"lazy\" /></figure>\n<figure class=\"note-figure\"><img src=\"/notes-media/java-web-024.png\" alt=\"WEB 标准与 HTML / CSS 笔记配图\" loading=\"lazy\" /></figure>\n<p>使用 function 关键字进行定义</p>\n<figure class=\"note-figure\"><img src=\"/notes-media/java-web-025.png\" alt=\"WEB 标准与 HTML / CSS 笔记配图\" loading=\"lazy\" /></figure>\n<pre class=\"note-code\">&lt; script &gt;</pre>\n<pre class=\"note-code\">function showMessage () {</pre>\n<p>alert ( ' 欢迎来到我的网站！ ' );}showMessage ();&lt;/ script &gt; &gt;</p>\n<h4>匿名函数</h4>\n<p>一种没有名称的函数</p>\n<figure class=\"note-figure\"><img src=\"/notes-media/java-web-026.png\" alt=\"WEB 标准与 HTML / CSS 笔记配图\" loading=\"lazy\" /></figure>\n<pre class=\"note-code\">let a = () =&gt; {</pre>\n<p>console . log ( 'a' )}</p>\n<pre class=\"note-code\">let b = ( c , d ) =&gt; {</pre>\n<p>console . log ( 'b' )</p>\n<h4>a ()</h4>\n<p>return c + d ;}</p>\n<pre class=\"note-code\">let e = b ( 20 , 30 );alert ( e );&lt;/ script &gt; &gt;自定义对象</pre>\n<figure class=\"note-figure\"><img src=\"/notes-media/java-web-027.png\" alt=\"WEB 标准与 HTML / CSS 笔记配图\" loading=\"lazy\" /></figure>\n<h4>定义方法的时候最好不用箭头法</h4>\n<p>Json js 对象标记法书写的文本作为数据载体 本质只是字符串</p>\n<figure class=\"note-figure\"><img src=\"/notes-media/java-web-028.png\" alt=\"WEB 标准与 HTML / CSS 笔记配图\" loading=\"lazy\" /></figure>\n<h4>DOM 文档对象模型</h4>\n<p>将标记语言的各个部分封装为对应的对象</p>\n<figure class=\"note-figure\"><img src=\"/notes-media/java-web-029.png\" alt=\"WEB 标准与 HTML / CSS 笔记配图\" loading=\"lazy\" /></figure>\n<figure class=\"note-figure\"><img src=\"/notes-media/java-web-030.png\" alt=\"WEB 标准与 HTML / CSS 笔记配图\" loading=\"lazy\" /></figure>\n<figure class=\"note-figure\"><img src=\"/notes-media/java-web-031.png\" alt=\"WEB 标准与 HTML / CSS 笔记配图\" loading=\"lazy\" /></figure>\n<p>DOM 操作的核心思想 将网页中的所有元素当作对象来处理</p>\n<figure class=\"note-figure\"><img src=\"/notes-media/java-web-032.png\" alt=\"WEB 标准与 HTML / CSS 笔记配图\" loading=\"lazy\" /></figure>\n<p>比如 h1 会封装为 heading 对象</p>\n<h4>innerHTML</h4>\n<p>querySelector 方法匹配的是对应的第一个元素</p>\n<pre class=\"note-code\">&lt; h1 id = &quot;title1&quot; &gt; 11111 &lt;/ h1 &gt;</pre>\n<pre class=\"note-code\">&lt; h1 &gt; 22222 &lt;/ h1 &gt;</pre>\n<pre class=\"note-code\">&lt; h1 &gt; 33333 &lt;/ h1 &gt;</pre>\n<pre class=\"note-code\">&lt; script &gt;</pre>\n<pre class=\"note-code\">let h1 = document . querySelector ( 'h1' );</pre>\n<p>h1 . innerHTML = '15444546' ;</p>\n<pre class=\"note-code\">let h1s = document . querySelectorAll ( 'h1' );</pre>\n<p>h1s [ 2 ]. innerHTML = '1fsd11' ;&lt;/ script &gt;</p>\n<h4>事件监听</h4>\n<p>语法 事件源 .addEventListener(' 事件类型 ', 事件触发的函数 );</p>\n<h4>1 事件源 谁出发时间</h4>\n<p>2 事件类型 怎么触发？</p>\n<p>3 执行的函数 要做什么事</p>\n<figure class=\"note-figure\"><img src=\"/notes-media/java-web-033.png\" alt=\"WEB 标准与 HTML / CSS 笔记配图\" loading=\"lazy\" /></figure>\n<p>如果是 addEventListener 就不会覆盖</p>\n<h4>vue 框架</h4>\n<p>vue 是一款用于构建用户界面的渐进式的 js 框架</p>\n<figure class=\"note-figure\"><img src=\"/notes-media/java-web-034.png\" alt=\"WEB 标准与 HTML / CSS 笔记配图\" loading=\"lazy\" /></figure>\n<p>优点 提升前端项目的开发效率缺点 需要理解记忆框架的使用规则</p>\n<h4>vue 的快速入门</h4>\n<p>基于数据渲染出用户看到的页面V ue 中的常用指令</p>\n<figure class=\"note-figure\"><img src=\"/notes-media/java-web-035.png\" alt=\"WEB 标准与 HTML / CSS 笔记配图\" loading=\"lazy\" /></figure>\n<pre class=\"note-code\">&lt;td&gt;&lt;img class=&quot;avatar&quot; src=&quot;{{e.image}}&quot;&gt; 这个是不可以的</pre>\n<figure class=\"note-figure\"><img src=\"/notes-media/java-web-036.png\" alt=\"WEB 标准与 HTML / CSS 笔记配图\" loading=\"lazy\" /></figure>\n<p>v-show 会渲染所有的值 如果条件不符合就设置 display:none</p>\n<figure class=\"note-figure\"><img src=\"/notes-media/java-web-037.png\" alt=\"WEB 标准与 HTML / CSS 笔记配图\" loading=\"lazy\" /></figure>\n<figure class=\"note-figure\"><img src=\"/notes-media/java-web-038.png\" alt=\"WEB 标准与 HTML / CSS 笔记配图\" loading=\"lazy\" /></figure>\n<figure class=\"note-figure\"><img src=\"/notes-media/java-web-039.png\" alt=\"WEB 标准与 HTML / CSS 笔记配图\" loading=\"lazy\" /></figure>\n<p>生命周期 ： 一个对象从创建到消失的时间</p>\n<figure class=\"note-figure\"><img src=\"/notes-media/java-web-040.png\" alt=\"WEB 标准与 HTML / CSS 笔记配图\" loading=\"lazy\" /></figure>\n<p>1 依赖管理 方便快捷的管理项目依赖的资源 (jar 包 )</p>\n<h4>Pom.xml 是配置文件</h4>\n<p>2 项目构建 标准化的跨平台的自动化项目构建方式</p>\n<p>3 统一项目结构 提供标准统一的项目结构、</p>\n<h4>一、 HTML &amp; CSS 前端基础</h4>\n<h4>1. 核心概念与关系</h4>\n<p>HTML ( 超文本标记语言 ) ：负责网页的 结构 。</p>\n<p>CSS ( 层叠样式表 ) ：控制网页的 样式 。</p>\n<h4>2. HTML 常见标签</h4>\n<p>标题标签 ： &lt;h1&gt; ~ &lt;h6&gt; （级别由高到低，字号由大到小）。</p>\n<p>超链接标签 ： &lt;a&gt;* href ：目标网址。</p>\n<p>target ： _blank （在新窗口打开）、 _self （在当前窗口打开，默认值）。</p>\n<h4>多媒体标签 ：</h4>\n<pre class=\"note-code\">&lt;video&gt; ：视频标签，属性包含 src （路径）、 controls （显示控制条）、 width / height （宽度 / 高度，单位为像素或父元素占比）。</pre>\n<p>行级通用容器 ： &lt;span&gt; ，无特殊语义，常用于对局部元素分组添加样式。</p>\n<h4>排版与文本格式化 ：</h4>\n<pre class=\"note-code\">&lt;p&gt; ：段落； &lt;br&gt; ：换行。</pre>\n<pre class=\"note-code\">&lt;b&gt; / &lt;strong&gt; ：加粗； &lt;i&gt; / &lt;em&gt; ：倾斜； &lt;s&gt; / &lt;del&gt; ：删除线； &lt;u&gt; / &lt;ins&gt; ：下划线。</pre>\n<p>字符实体 ： &amp;nbsp; （不断行空格）、 &amp;lt; （小于号 &lt; ）、 &amp;gt; （大于号 &gt; ）。</p>\n<p>3. CSS 引入方式与语法</p>\n<h4>引入方式 ：</h4>\n<p>行内样式 ：写在标签的 style 属性中。</p>\n<p>内部样式 ：写在 &lt;style&gt; 标签中（一般位于 &lt;head&gt; 内）。</p>\n<p>外部样式 ：写在单独的 .css 文件中，通过 &lt;link rel=&quot;stylesheet&quot; href=&quot; 路径 &quot;&gt; 连接，复用性高。</p>\n<h4>CSS 选择器与优先级 ：</h4>\n<p>类选择器 (.className) ：每个元素可以有多个类名，用空格隔开；相同属性时后面的 class 会覆盖前面的。</p>\n<p>ID 选择器 (#idName) ：每个元素只能有一个唯一 ID 。</p>\n<p>优先级 ： ID 选择器 &gt; 类选择器。</p>\n<p>其他：分组选择器、属性选择器。</p>\n<h4>颜色表示方式 ：</h4>\n<p>1. 关键字（如 red, green ）</p>\n<h4>2. rgb(r, g, b)</h4>\n<p>3. rgba(r, g, b, a) （ a 代表透明度）</p>\n<p>4. 十六进制（如 #rrggbb ）---</p>\n<h4>二、 盒子模型与页面布局</h4>\n<p>1. 盒子模型 (Box Model)</p>\n<h4>从外到内依次为：</h4>\n<p>margin （外边距） ：控制盒子与外部元素之间的距离。</p>\n<p>border （边框） ：盒子的边框。</p>\n<p>padding （内边距） ：控制内容与边框之间的距离。</p>\n<p>content （内容） ：盒子的实际内容区域。</p>\n<h4>2. 块级元素 vs 行内元素</h4>\n<p>块级元素（如 &lt;div&gt; ） ：独占一行，默认宽度为父元素宽度，高度由内容撑开，可设置宽高。</p>\n<p>行内元素（如 &lt;span&gt; ） ：一行可显示多个，默认宽度由内容撑开，不可直接设置宽高。</p>\n<h4>3. Flex 弹性布局</h4>\n<p>给容器添加 display: flex; 属性即可开启弹性布局。</p>\n<p>flex-direction （设置主轴方向） ：</p>\n<p>row ：水平向右（默认）。</p>\n<p>column ：垂直向下。</p>\n<p>justify-content （子元素在主轴上的对齐方式） ：</p>\n<p>flex-start ：从头开始排列。</p>\n<p>flex-end ：从尾部开始排列。</p>\n<p>center ：居中对齐。</p>\n<p>space-around ：平分剩余空间（两端有留白）。</p>\n<p>space-between ：先两边贴边，再平分剩余空间。</p>\n<h4>1. HTML 表单 (&lt;form&gt;)</h4>\n<p>主要属性 ： action （发送地址）、 method （提交方式，如 GET / POST ）。</p>\n<h4>常见表单项 ：</h4>\n<pre class=\"note-code\">&lt;select&gt; ：下拉列表。</pre>\n<pre class=\"note-code\">&lt;textarea&gt; ：多行文本域。</pre>\n<pre class=\"note-code\">&lt;input type=&quot;...&quot;&gt; 常用类型：</pre>\n<p>text （文本框，默认）、 password （密码框）、 radio （单选）、 checkbox （复选框）、 file （文件上传）、 date/time/datetime-local （日期时间）、 number （数字）、 email （邮箱）、 hidden （隐藏域）、 submit/reset/button （提交 / 重置 / 自定义按钮）。</p>\n<p>常用属性： name （提交参数名）、 placeholder （占位提示文本）。</p>\n<h4>将 HTML 抽象为树形结构：</h4>\n<p>Document ：整个文档对象。</p>\n<p>Element ：元素节点。</p>\n<p>Attribute ：属性节点； Text ：文本节点； Comment ：注释节点。</p>\n<h4>选择器获取元素 ：</h4>\n<p>document.querySelector('selector') ：获取匹配的第一个元素。</p>\n<p>document.querySelectorAll('selector') ：获取所有匹配的元素列表。</p>\n<h4>2. 事件监听</h4>\n<pre class=\"note-code\">语法 ： 事件源 .addEventListener(' 事件类型 ', 回调函数 ) （可绑定多个监听，不覆盖） 或 事件源 .on 事件名 = function(){} 。</pre>\n<h4>常见事件 ：</h4>\n<p>鼠标事件 ： click （点击）、 mouseenter （移入）、 mouseleave （移出）。</p>\n<p>键盘事件 ： keydown （按下）、 keyup （抬起）。</p>\n<p>焦点事件 ： focus （获得焦点）、 blur （失去焦点）。</p>\n<p>表单事件 ： input （输入时触发）、 submit （提交时触发）。</p>"
      },
      {
        "id": "js-async",
        "title": "JavaScript 与异步请求",
        "summary": "JavaScript 语法、Vue 指令、Ajax / Axios 异步请求。",
        "html": "<h4>HTML （结构） CSS （表现） JS （行为）</h4>\n<h4>html 超文本标记语言</h4>\n<p>标记语言 由标签 &lt; 标签名 &gt; 构成的语言</p>\n<p>html 的标签是预定义好的 比如 &lt;h1&gt; 展示标题 &lt;img&gt; 展示图片 &lt;video&gt; 展示视频 代码直接在浏览器运行 html 由浏览器解析</p>\n<h4>2. JavaScript (JS) 语法基础</h4>\n<p>位置 ：写在 &lt;script&gt; 标签内，一般建议放在 &lt;body&gt; 底部；或引入外部文件 &lt;script src=&quot;...&quot;&gt;&lt;/script&gt; 。</p>\n<h4>输出方式 ：</h4>\n<p>alert(...) ：弹出提示框。</p>\n<p>document.write(...) ：在页面文档中输出。</p>\n<p>console.log(...) ：输出到浏览器控制台。</p>\n<h4>变量声明 ：</h4>\n<pre class=\"note-code\">let ：声明变量（弱类型，局部 / 块级作用域）。</pre>\n<pre class=\"note-code\">var ：早期声明变量方式，不够严谨。</pre>\n<pre class=\"note-code\">const ：声明常量。</pre>\n<p>数据类型检测 (typeof) ：</p>\n<p>number （数字，不分整型 / 浮点型）、 string （字符串）、 boolean （布尔值）、 object （对象 /null ）、 undefined （未定义）。</p>\n<pre class=\"note-code\">模板字符串 ：使用反引号 ，拼接变量语法： ${ 变量名 } 。</pre>\n<h4>自定义函数与对象 ：</h4>\n<pre class=\"note-code\">函数定义： function 函数名 ( 形参 ) { return ...; } 。</pre>\n<pre class=\"note-code\">对象字面量： let obj = { 属性名 : 值 , 方法名 : function(){} }; 。</pre>\n<h4>五、 Vue &amp; Ajax / Axios</h4>\n<h4>1. Vue 基础指令与模块化</h4>\n<p>模块化 ： import 元素 from ' 路径 ' ，需要设置 &lt;script type=&quot;module&quot;&gt; 。</p>\n<h4>常用指令 ：</h4>\n<p>v-for ：列表循环遍历。</p>\n<p>v-bind ：动态绑定属性值（如 v-bind:href=&quot; 变量 &quot; 可简写为 :href=&quot; 变量 &quot; ）。注：插值表达式不能直接写在标签属性内部。</p>\n<p>v-if / v-else ：条件渲染。</p>\n<p>v-model ：表单双向数据绑定（数据变 ↔ 页面更新）。</p>\n<p>v-on ：绑定事件，简写为 @ 事件名 （如 @click ）。</p>\n<p>方法定义 ：写在应用实例的 methods 对象中， methods 中的 this 指向 Vue 实例，可直接访问 data 中定义的数据。</p>\n<h4>2. Ajax &amp; Axios</h4>\n<p>Ajax ：异步 JavaScript 与 XML ，实现无刷新异步数据交换。</p>\n<p>同步 vs 异步 ：同步发起请求后需等待服务器响应才能继续；异步发起请求后可继续其他操作。</p>\n<p>Axios ：对原生 Ajax 的封装。</p>\n<p>请求配置： method （请求方式）、 url （路径）、 data （请求体数据）、 params （ URL 拼接参数）。</p>\n<pre class=\"note-code\">使用方式： axios. 请求方式 (url, data/config).then(res =&gt; {}).catch(err =&gt; {}) 。</pre>\n<p>async / await ：将异步代码写成同步风格， await 必须在 async 函数内部使用。</p>"
      },
      {
        "id": "http-session",
        "title": "HTTP / Cookie / Session / JWT",
        "summary": "HTTP 无状态、Cookie / Session、JWT 登录校验。",
        "html": "<h4>需要额外的 HTTP 请求（小项目可忽略）</h4>\n<p>需要额外的 HTTP 请求（小项目可忽略）News.css 中</p>\n<h4>span {</h4>\n<p>color : aqua ;}</p>\n<h4>在 html 文件中</h4>\n<pre class=\"note-code\">&lt; link rel = &quot;stylesheet&quot; href = &quot;news.css&quot; &gt;&lt;/ link &gt;</pre>\n<figure class=\"note-figure\"><img src=\"/notes-media/java-web-005.png\" alt=\"HTTP / Cookie / Session / JWT 笔记配图\" loading=\"lazy\" /></figure>\n<pre class=\"note-code\">&lt; style &gt;</pre>\n<h4>span {</h4>\n<p>color : #303b30 ;}&lt;/ style &gt;</p>\n<figure class=\"note-figure\"><img src=\"/notes-media/java-web-006.png\" alt=\"HTTP / Cookie / Session / JWT 笔记配图\" loading=\"lazy\" /></figure>\n<h4>在 head 中</h4>\n<h4>.cls {</h4>\n<p>color : rgb ( 178 , 178 , 178 );}&lt;/ style &gt;</p>\n<h4>在 body 中</h4>\n<pre class=\"note-code\">&lt; span class = &quot;.cls&quot; &gt; 2024 年 5 月 1 日 &lt;/ span &gt;</pre>\n<h4>在 head 中</h4>\n<h4>/* .cls {</h4>\n<p>color: rgb(178,178,178);} */</p>\n<h4>#time {</h4>\n<p>color : rgb ( 123 , 42 , 123 );}&lt;/ style &gt;</p>\n<h4>在 body 中</h4>\n<pre class=\"note-code\">&lt; span class = &quot;.cls&quot; id = &quot;time&quot; &gt; 2024 年 5 月 1 日 &lt;/ span &gt;</pre>\n<h4>七、 HTTP 协议与 MVC 架构</h4>\n<h4>1. HTTP 协议特性</h4>\n<p>1. 基于 TCP 协议 ，面向连接，传输安全。</p>\n<p>2. 请求 - 响应模型 ：一请求对应一响应。</p>\n<p>3. 无状态协议 ：每次请求 / 响应都是独立的，无法直接在多个请求间共享状态（如购物车数据）。</p>\n<h4>2. GET 与 POST 的区别</h4>\n<p>| 特性 | GET 请求 | POST 请求 |</p>\n<p>| --- | --- | --- |</p>\n<p>| 参数位置 | 拼接在请求行（ URL 后面） | 放在请求体中 |</p>\n<p>| 数据大小 | 受限（ URL 长度限制） | 理论上不受限 |</p>\n<p>| 安全性 | 较低（参数直接暴露） | 相对较高 |</p>\n<h4>3. 三层架构设计（软件设计模式）</h4>\n<p>为了实现 高内聚，低耦合 （组件内部元素高度紧密，模块之间关联尽量降低）以及单职责原则：</p>\n<h4>会话技术：用户登录成功之后，在后续的每一次请求中，都可以获取到该标记。</h4>\n<h4>会话跟踪技术有两种：</h4>\n<p>Cookie （客户端会话跟踪技术）：数据存储在客户</p>\n<h4>端浏览器当中</h4>\n<p>Session （服务端会话跟踪技术）：数据存储在储在服务端</p>\n<h4>令牌技术</h4>\n<p>我们使用 cookie 来跟踪会话，我们就可以在浏览器第一次发起请求来请求服务器的时候，我们在服务器端来设置一个 cookie 。</p>\n<p>服务器会 自动 的将 cookie 响应给浏览器。</p>\n<p>浏览器接收到响应回来的数据之后，会 自动 的将cookie 存储在浏览器本地。</p>\n<p>在后续的请求当中，浏览器会 自动 的将 cookie 携带到服务器端。</p>\n<p>为什么这一切都是自动化进行的？</p>\n<p>是因为 cookie 它是 HTP 协议当中所支持的技术，而各大浏览器厂商都支持了这一标准。在 HTTP 协议官方给我</p>\n<h4>们提供了一个响应头和请求头：</h4>\n<p>响应头 Set-Cookie ：设置 Cookie 数据的</p>\n<p>请求头 Cookie ：携带 Cookie 数据的</p>\n<figure class=\"note-figure\"><img src=\"/notes-media/java-web-046.png\" alt=\"HTTP / Cookie / Session / JWT 笔记配图\" loading=\"lazy\" /></figure>\n<p>缺点：</p>\n<p>移动端 APP(Android 、 IOS) 中无法使用 Cookie</p>\n<p>不安全，用户可以自己禁用 Cookie</p>\n<h4>Cookie 不能跨域</h4>\n<p>现在的项目，大部分都是前后端分离的，前后端最终也会分开部署，前端部署在服务器 192.168.150.200 上，端口 80 ，后端部署在 192.168.150.100 上，端口 8080</p>\n<p>我们打开浏览器直接访问前端工程，访问 url ： http://192.168.150.200/login.html</p>\n<p>然后在该页面发起请求到服务端，而服务端所在地址不再是 localhost ，而是服务器的 IP 地址 192.168.150.100 ，假设访问接口地址为： http://192.168.150.100:8080/login</p>\n<p>那此时就存在跨域操作了，因为我们是在 http://192.168.150.200/login.html 这个页面上访问了 http://192.168.150.100:8080/login 接口</p>\n<p>此时如果服务器设置了一个 Cookie ，这个 Cookie 是不能使用的，因为 Cookie 无法跨域</p>\n<figure class=\"note-figure\"><img src=\"/notes-media/java-web-047.png\" alt=\"HTTP / Cookie / Session / JWT 笔记配图\" loading=\"lazy\" /></figure>\n<h4>协议</h4>\n<p>IP/ 协议</p>\n<h4>端口</h4>\n<p>我们提到 Session ，它是服务器端会话跟踪技术，所以它是存储在服务器端的。而 Session 的底层其实就是基于我们刚才所介绍的 Cookie 来实现的。</p>\n<p>获取 Session</p>\n<figure class=\"note-figure\"><img src=\"/notes-media/java-web-048.png\" alt=\"HTTP / Cookie / Session / JWT 笔记配图\" loading=\"lazy\" /></figure>\n<p>如果我们现在要基于 Session 来进行会话跟踪，浏览器在第一次请求服务器的时候，我们就可以直接在服务器当中来获取到会话对象 Session 。如果是第一次请求 Session ，会话对象是不</p>\n<p>存在的，这个时候服务器会自动的创建一个会话对象 Session 。而每一个会话对象 Session ，它都有一个 ID （示意图中 Session 后面括号中的 1 ，就表示 ID ），我们称之为 Session 的 ID 。</p>\n<p>响应 Cookie (JSESSIONID)</p>\n<figure class=\"note-figure\"><img src=\"/notes-media/java-web-049.png\" alt=\"HTTP / Cookie / Session / JWT 笔记配图\" loading=\"lazy\" /></figure>\n<p>接下来，服务器端在给浏览器响应数据的时候，它会将 Session 的 ID 通过 Cookie 响应给浏览器。其实在响应头当中增加了一个 Set-Cookie 响应头。这个 Set-Cookie 响应头对应的值是不是</p>\n<p>cookie？ cookie 的名字是固定的 JSESSIONID 代表的服务器端会话对象 Session 的 ID。浏览器会自动识别这个响应头，然后自动将Cookie存储在浏览器本地。</p>\n<p>查找 Session</p>\n<figure class=\"note-figure\"><img src=\"/notes-media/java-web-050.png\" alt=\"HTTP / Cookie / Session / JWT 笔记配图\" loading=\"lazy\" /></figure>\n<p>接下来，在后续的每一次请求当中，都会将 Cookie 的数据获取出来，并且携带到服务端。接下来服务器拿到 JSESSIONID 这个 Cookie 的值，也就是 Session 的 ID 。拿到 ID 之后，就会从众</p>\n<p>多的 Session 当中来找到当前请求对应的会话对象 Session 。</p>\n<p>优点： Session 是存储在服务端的，安全缺点：</p>\n<h4>服务器集群环境下无法直接使用 Session</h4>\n<p>移动端 APP(Android 、 IOS) 中无法使用 Cookie用户可以自己禁用 Cookie</p>\n<h4>Cookie 不能跨域</h4>\n<pre class=\"note-code\">PS ： Session 底层是基于 Cookie 实现的会话跟踪，如果 Cookie 不可用，则该方案，也就失效了。</pre>\n<h4>为什么 session 用不了集群服务器</h4>\n<p>用户第一次请求，负载均衡把请求打到服务器 A ； A 生成 sessionId ，放在响应 Cookie 返回给浏览器， Session 数据只存在 A 的内存里 。 第二次请求，浏览器自动带上 Cookie 里的</p>\n<p>sessionId；如果负载均衡把请求分到服务器 B。 B 本地内存 没有这个 sessionId 对应的会话数据 ，就识别不到用户，会话丢失，需要重新登录。</p>\n<p>第三种 令牌</p>\n<h4>令牌技术实现会话跟踪：</h4>\n<p>用户调用登录接口，登录成功后服务端生成令牌，作为用户身份凭证，返回给前端；</p>\n<p>前端拿到令牌，保存到 Cookie 或者 localStorage 等本地存储；</p>\n<p>前端后续每次发起请求，都带上这个令牌传给服务端；</p>\n<p>服务端接收请求后校验令牌是否有效，有效则认定用户已登录，无效则判定未登录；</p>\n<p>会话内需要共享的数据，可以直接存入令牌本身，不需要服务端额外保存会话信息。</p>\n<p>优缺点优点：</p>\n<h4>减轻服务器的存储压力（无需在服务器端存储）</h4>\n<p>缺点：需要自己实现（包括令牌的生成、令牌的传递、令牌的校验）</p>\n<h4>JWT 令牌最典型的应用场景就是登录认证：</h4>\n<p>在浏览器发起请求来执行登录操作，此时会访问登录的接口，如果登录成功之后，我们需要生成一个 jwt 令牌，将生成的 jwt 令牌返回给前端。</p>\n<p>前端拿到 jwt 令牌之后，会将 jwt 令牌存储起来。在后续的每一次请求中都会将 jwt 令牌携带到服务端。</p>\n<p>服务端统一拦截请求之后，先来判断一下这次请求有没有把令牌带过来，如果没有带过来，直接拒绝访问，如果带过来了，还要校验一下令牌是否是有效。如果有效，就直接放行进行请求的处理。</p>\n<p>在 JWT 登录认证的场景中我们发现，整个流程当中涉及到两步操作：在登录成功之后，要生成令牌。</p>\n<p>每一次请求当中，要接收令牌并对令牌进行校验。</p>\n<p>JWT 全称 JSON Web Token （官网： https://jwt.io/ ），定义了一种简洁的、自包含的格式，用于在通信双方以 json 数据格式安全的传输信息。由于数字签名的存在，这些信息是可靠的。</p>\n<p>简洁：是指 jwt 就是一个简单的字符串 。可以在请求参数或者是请求头当中直接传递。</p>\n<p>自包含 ：指的是 jwt 令牌，看似是一个随机的字符串，但是 我们是可以根据自身的需求在 jwt 令牌中存储自定义的数据内容。 如：可以直接在 jwt 令牌中存储用户的相关信息。</p>\n<p>简单来讲， jwt 就是将原始的 json 数据格式进行了安全的封装，这样就可以直接基于 jwt 在通信双方安全的进行信息传输了。</p>\n<h4>1 header 头 记录令牌类型 签名算法等</h4>\n<p>2 payload 有效载荷 自定义信息 有效信息等</p>\n<h4>3 signature 安全性 防止被篡改</h4>\n<p>签名的目的就是为了防 jwt 令牌被篡改，而正是因为 jwt 令牌最后一个部分数字签名的存在，所以整个 jwt 令牌是非常安全可靠的。一旦 jwt 令牌当中任何一个部分、任何一个字符被篡改</p>\n<p>了，整个令牌在校验的时候都会失败，所以它是非常安全可靠的。</p>\n<h4>通过以上测试，我们在使用 JWT 令牌时需要注意：</h4>\n<p>JWT 校验时使用的签名秘钥，必须和生成 JWT 令牌时使用的秘钥是配套的。</p>\n<p>如果 JWT 令牌解析校验时报错，则说明 JWT 令牌被篡改 或 失效了，令牌非法。</p>\n<h4>所有的请求，拦截到了之后，都需要校验令牌吗 ？</h4>\n<h4>答案： 登录请求例外</h4>\n<p>拦截到请求后，什么情况下才可以放行，执行业务操作 ？</p>\n<p>答案： 有令牌，且令牌校验通过 ( 合法 ) ；否则都返回未登录错误结果</p>\n<h4>获取请求 url</h4>\n<p>判断请求 url 中是否包含 login ，如果包含，说明是登录操作，放行</p>\n<h4>判断令牌是否存在，如果不存在，响应 401</h4>\n<p>解析 token ，如果解析失败，响应 401</p>\n<h4>放行</h4>\n<pre class=\"note-code\">@WebFilter(urlPatterns = &quot;/*&quot;) // 配置过滤器要拦截的请求路径（ /* 表示拦截浏览器的所有请求 ）</pre>\n<p>当我们在 Filter 类上面加了 @WebFilter 注解之后，接下来我们还需要在启动类上面加上一个注解 @ServletComponentScan ，通过这个 @ServletComponentScan 注解来开启 SpringBoot 项目对于Servlet组件的支持。</p>\n<p>过滤器当中我们拦截到了请求之后，如果希望继续访问后面的 web 资源，就要执行放行操作，放行就是调用 FilterChain 对象当中的 doFilter() 方法，在调用 doFilter() 这个方法之前所编写的代码属于放行之前的逻辑。</p>\n<p>拦截具体路径</p>\n<h4>/login</h4>\n<p>只有访问 /login 路径时，才会被拦截</p>\n<h4>/emps/*</h4>\n<p>访问 /emps 下的所有资源，都会被拦截拦截所有/*</p>\n<h4>访问所有资源，都会被拦截</h4>\n<p>最后我们在来介绍下过滤器链，什么是过滤器链呢？所谓过滤器链指的是在一个 web 应用程序当中，可以配置多个过滤器，多个过滤器就形成了一个过滤器链。</p>\n<figure class=\"note-figure\"><img src=\"/notes-media/java-web-051.png\" alt=\"HTTP / Cookie / Session / JWT 笔记配图\" loading=\"lazy\" /></figure>\n<p>比如：在我们 web 服务器当中，定义了两个过滤器，这两个过滤器就形成了一个过滤器链。</p>\n<p>而这个链上的过滤器在执行的时候会一个一个的执行，会先执行第一个 Filter ，放行之后再来执行第二个 Filter ，如果执行到了最后一个过滤器放行之后，才会访问对应的 web 资源。</p>\n<p>访问完 web 资源之后，按照我们刚才所介绍的过滤器的执行流程，还会回到过滤器当中来执行过滤器放行后的逻辑，而在执行放行后的逻辑的时候，顺序是反着的。</p>\n<p>先要执行过滤器 2 放行之后的逻辑，再来执行过滤器 1 放行之后的逻辑，最后在给浏览器响应数据</p>\n<p>过滤器链上过滤器的执行顺序：注解配置的 Filter ，优先级是按照过滤器类名（字符串）的自然排序。 比如：</p>\n<h4>每个会话范围内会创建新的实例（ web 环境中，了解）</h4>\n<h4>application</h4>\n<p>每个应用范围内会创建新的实例（ web 环境中，了解）</p>"
      },
      {
        "id": "filter-interceptor",
        "title": "Filter 过滤器与拦截器",
        "summary": "Filter 与 Spring Interceptor 统一拦截。",
        "html": "<h4>统一拦截技术：过滤器 Filter 、拦截器 Interceptor</h4>\n<p>会话跟踪： 一种维护浏览器状态的方法，服务器需要识别多次请求是否来自于同一浏览器，以便在同一次会话</p>\n<h4>的多次请求间共享数据</h4>\n<p>如果是同一个浏览器发出来的，就说明是同一个会话。如果是不同的浏览器发出来的，就说明是不同的会话。而识别多次请求是否来自于同一浏览器的过程，我们就称为会话跟踪</p>\n<h4>什么是 Filter ？</h4>\n<p>Filter 表示过滤器，是 JavaWeb 三大组件 (Servlet 、 Filter 、 Listener) 之一。</p>\n<p>过滤器可以把对资源的请求拦截下来，从而实现一些特殊的功能</p>\n<p>使用了过滤器之后，要想访问 web 服务器上的资源，必须先经过滤器，过滤器处理完毕之后，才可以访问对应的资源。</p>\n<p>过滤器一般完成一些通用的操作，比如：登录校验、统一编码处理、敏感字符处理等。</p>\n<p>要进入到后台管理系统，我们必须先完成登录操作，此时就需要访问登录接口 login 。</p>\n<p>登录成功之后，我们会在服务端生成一个 JWT 令牌，并且把 JWT 令牌返回给前端，前端会将 JWT 令牌存储下来。</p>\n<p>在后续的每一次请求当中，都会将 JWT 令牌携带到服务端，请求到达服务端之后，要想去访问对应的业务功能，此时我们必须先要校验令牌的有效性。</p>\n<p>对于校验令牌的这一块操作，我们使用登录校验的过滤器，在过滤器当中来校验令牌的有效性。如果令牌是无效的，就响应一个错误的信息，也不会再去放行访问对应的资源了。如果</p>\n<p>令牌存在，并且它是有效的，此时就会放行去访问对应的 web 资源，执行相应的业务操作。</p>\n<h4>DemoFilter</h4>\n<p>这两个过滤器来说， AbcFilter 会先执行， DemoFilter 会后执行。</p>\n<p>什么是拦截器？</p>\n<p>是一种动态拦截方法调用的机制，类似于过滤器。</p>\n<p>拦截器是 Spring 框架中提供的，用来动态拦截控制器方法的执行。</p>\n<p>拦截器的作用：拦截请求，在指定方法调用前后，根据业务需要执行预先设定的代码。</p>\n<figure class=\"note-figure\"><img src=\"/notes-media/java-web-052.png\" alt=\"Filter 过滤器与拦截器 笔记配图\" loading=\"lazy\" /></figure>\n<p>拦截器只拦截 Controller 控制器方法（ Java 后端接口方法），它 拦截的是 Java 方法执行，不是网络文件请求 ，这种针对方法层面的拦截就叫动态拦截方法调用机制。</p>\n<p>preHandle 方法：目标资源方法执行前执行。 返回 true ：放行 返回 false ：不放行</p>\n<p>postHandle 方法：目标资源方法执行后执行</p>\n<p>afterCompletion 方法：视图渲染完毕后执行，最后执行</p>\n<p>在 Spring MVC 中，写好拦截器之后，必须把它注册到 Spring 的拦截器链里，它才会生效。注册的方式就是创建一个配置类，实现 WebMvcConfigurer 接口，重写 addInterceptors 方法。</p>\n<h4>拦截路径</h4>\n<p>首先我们先来看拦截器的拦截路径的配置，在注册配置拦截器的时候，我们要指定拦截器的拦截路径，通过 addPathPatterns(&quot; 要拦截路径 &quot;) 方法，就可以指定要拦截哪些资源。</p>\n<p>在入门程序中我们配置的是 / ，表示拦截所有资源，而在配置拦截器时，不仅可以指定要拦截哪些资源，还可以指定不拦截哪些资源，只需要调用 excludePathPatterns(&quot; 不拦截路径 &quot;) 方法，指定哪些资源不需要拦截。</p>\n<pre class=\"note-code\">@Configuration public class WebConfig implements WebMvcConfigurer {</pre>\n<p>// 拦截器对象 @Autowired private DemoInterceptor demoInterceptor;</p>\n<pre class=\"note-code\">@Override public void addInterceptors(InterceptorRegistry registry) { // 注册自定义拦截器对象 registry.addInterceptor(demoInterceptor) .addPathPatterns(&quot;/&quot;)// 设置拦截器拦截的请求路径（ / 表示拦截所有请求） .excludePathPatterns(&quot;/login&quot;);// 设置不拦截的请求路径 } }/*</pre>\n<h4>一级路径</h4>\n<p>能匹配 /depts ， /emps ， /login ，不能匹配 /depts/1/</p>\n<h4>任意级路径</h4>\n<p>能匹配 /depts ， /depts/1 ， /depts/1/2</p>\n<h4>/depts 下的一级路径</h4>\n<p>能匹配 /depts/1 ，不能匹配 /depts/1/2 ， /depts</p>\n<h4>/depts 下的任意级路径</h4>\n<p>能匹配 /depts ， /depts/1 ， /depts/1/2 ，不能匹配 /emps/1</p>"
      },
      {
        "id": "spring-boot",
        "title": "Spring Boot 起步依赖与自动配置",
        "summary": "Maven 仓库、起步依赖、自动配置与分模块。",
        "html": "<h4>Maven 基于项目对象模型的概念 通过一小段描述信息来管理项目的构建</h4>\n<figure class=\"note-figure\"><img src=\"/notes-media/java-web-041.png\" alt=\"Spring Boot 起步依赖与自动配置 笔记配图\" loading=\"lazy\" /></figure>\n<h4>1 本地仓库 ： 自己计算机上的一个目录</h4>\n<p>2 中央仓库 maven 团队维护的 全球唯一的仓库</p>\n<p>3 远程仓库 （私服） ：一般由公司团队搭建的私有仓库</p>\n<figure class=\"note-figure\"><img src=\"/notes-media/java-web-042.png\" alt=\"Spring Boot 起步依赖与自动配置 笔记配图\" loading=\"lazy\" /></figure>\n<figure class=\"note-figure\"><img src=\"/notes-media/java-web-043.png\" alt=\"Spring Boot 起步依赖与自动配置 笔记配图\" loading=\"lazy\" /></figure>\n<figure class=\"note-figure\"><img src=\"/notes-media/java-web-044.png\" alt=\"Spring Boot 起步依赖与自动配置 笔记配图\" loading=\"lazy\" /></figure>\n<h4>六、 Maven 与 Spring Boot 基础</h4>\n<h4>1. Maven 项目构建工具</h4>\n<p>POM (Project Object Model) ：项目对象模型，核心配置文件为 pom.xml 。</p>\n<p>依赖管理 (&lt;dependencies&gt;) ：可以引入依赖，也可通过 &lt;exclusions&gt; 排除依赖。</p>\n<h4>版本号与仓库 ：</h4>\n<p>SNAPSHOT ：快照版（开发中、不固定）。</p>\n<p>RELEASE ：发行版（稳定）。</p>\n<p>依赖查找顺序：本地仓库 → 中央仓库。</p>\n<p>构建生命周期 ：包含 clean, default (compile, test, package, install...), site 。</p>\n<p>执行后面的生命周期，前面依赖的生命周期会自动触发执行。</p>\n<h4>2. Spring Boot 控制器注解</h4>\n<pre class=\"note-code\">@RestController ：标识为请求处理类（结合了 @Controller 与 @ResponseBody ）。</pre>\n<pre class=\"note-code\">@RequestMapping(&quot; 路径 &quot;) ：映射请求路径。</pre>\n<h4>十、 Spring Boot 请求传参与 Nginx 代理</h4>\n<p>1. 常用映射注解与请求参数获取</p>\n<h4>明确请求方式注解 ：</h4>\n<pre class=\"note-code\">@GetMapping / @PostMapping / @PutMapping / @DeleteMapping 。</pre>\n<h4>在 SpringBoot 中，文件上传时默认单个文件最大大小为 1M</h4>\n<p>那么如果需要上传大文件，可以在 application.properties 进行如下配置：</p>\n<p>spring ： servlet: multipart: max-file-size: 10MB max-request-size: 100MB</p>\n<p>如果直接存储在服务器的磁盘目录中，存在以下缺点：</p>\n<h4>不安全：磁盘如果损坏，所有的文件就会丢失</h4>\n<p>容量有限：如果存储大量的图片，磁盘空间有限 ( 磁盘不可能无限制扩容 )</p>\n<h4>为了解决上述问题呢，通常有两种解决方案：</h4>\n<p>自己搭建存储服务器，如： fastDFS 、 MinIO</p>\n<h4>使用现成的云服务，如：阿里云，腾讯云，华为云</h4>\n<p>set OSS_ACCESS_KEY_ID=（已隐藏）</p>\n<h4>在以上代码中，需要替换的内容为：</h4>\n<p>endpoint ：阿里云 OSS 中的 bucket 对应的域名</p>\n<h4>bucketName ： Bucket 名称</h4>\n<p>objectName ：对象名称，在 Bucket 中存储的对象的名称</p>\n<h4>region ： bucket 所属区域</h4>\n<p>如果只有一两个属性需要注入，而且不需要考虑复用性，使用 @Value 注解就可以了。</p>\n<h4>Spring 提供的简化方式套路：</h4>\n<p>1). 需要创建一个实现类，且实体类中的属性名和配置文件当中 key 的名字必须要一致</p>\n<p>比如：配置文件当中叫 endpoint ，实体类当中的属性也得叫 endpoint ，另外实体类当中的属性还需要提供 getter / setter 方法</p>\n<p>2). 需要将实体类交给 Spring 的 IOC 容器管理，成为 IOC 容器当中的 bean 对象</p>\n<p>3). 在实体类上添加 @ConfigurationProperties 注解，并通过 perfect 属性来指定配置参数项的前缀</p>\n<p>Mybatis 中封装查询结果，什么时候用 resultType ，什么时候用 resultMap ？</p>\n<p>如果查询返回的字段名与实体的属性名可以直接对应上，用 resultType 。</p>\n<p>如果查询返回的字段名与实体的属性名对应不上，或实体属性比较复杂，可以通过 resultMap 手动封装 。</p>\n<h4>全局异常处理器</h4>\n<p>我们该怎么样定义全局异常处理器？</p>\n<p>定义全局异常处理器非常简单，就是定义一个类，在类上加上一个注解 @RestControllerAdvice ，加上这个注解就代表我们定义了一个全局异常处理器。</p>\n<p>在全局异常处理器当中，需要定义一个方法来捕获异常，在这个方法上需要加上注解 @ExceptionHandler 。通过 @ExceptionHandler 注解当中的 value 属性来指定我们要捕获的是哪一类型的异常。</p>\n<pre class=\"note-code\">@RestControllerAdvice = @ControllerAdvice + @ResponseBody</pre>\n<p>处理异常的方法返回值会转换为 json 后再响应给前端</p>\n<p>我们已经完成了基础登录功能的开发与测试，在我们登录成功后就可以进入到后台管理系统中进行数据的操作。</p>\n<p>但是当我们在浏览器中新的页面上输入地址： http://localhost:90 ，发现没有登录仍然可以进入到后端管理系统页面。</p>\n<figure class=\"note-figure\"><img src=\"/notes-media/java-web-045.png\" alt=\"Spring Boot 起步依赖与自动配置 笔记配图\" loading=\"lazy\" /></figure>\n<p>而真正的登录功能应该是：登陆后才能访问后端系统页面，不登陆则跳转登陆页面进行登陆。</p>\n<p>为什么会出现这个问题？其实原因很简单，就是因为针对于我们当前所开发的部门管理、员工管理以及文件上传等相关接口来说，我们在服务器端并没有做任何的判断，没有去判断用户是否登录了。所以无论用户是否登录，都可以访问部门管理以及员工管理的相关数据。所以我们目前所开发的登录功能，它只是徒有其表。而我们要想解决这个问题，我们就需要完成一步非常重要的操作：登录校验。</p>\n<p>那应该怎么来实现登录校验的操作呢？具体的实现思路可以分为两部分：</p>\n<p>在员工登录成功后，需要将用户登录成功的信息存起来，记录用户已经登录成功的标记。</p>\n<p>在浏览器发起请求时，需要在服务端进行统一拦截，拦截后进行登录校验。</p>\n<p>为了简化这块操作，我们可以使用一种技术：统一拦截技术。</p>\n<p>通过统一拦截的技术，我们可以来拦截浏览器发送过来的所有的请求，拦截到这个请求之后，就可以通过请求来获取之前所存入的登录标记，在获取到登录标记且标记为登录成功，就说明员工已经登录了。如果已经登录，我们就直接放行 ( 意思就是可以访问正常的业务接口了 )</p>\n<p>我们要完成以上操作，会涉及到 web 开发中的两个技术：</p>\n<p>SpringBoot 为了增强程序的扩展性，除了支持配置文件的配置方式以外，还支持另外两种常见的配置方式：</p>\n<p>Java 系统属性配置 （格式： -Dkey=value ）</p>\n<h4>-Dserver.port=9000</h4>\n<p>命令行参数 （格式： --key=value ）</p>\n<h4>--server.port=10010</h4>\n<p>五种配置方式的优先级： 命令行参数 &gt; 系统属性参数 &gt; properties 参数 &gt; yml 参数 &gt; yaml 参数</p>\n<p>在 SpringBoot 项目当中，常见的属性配置方式有 5 种， 3 种配置文件，加上 2 种外部属性的配置 (Java 系统属性、命令行参数 ) 。通过以上的测试，我们也得出了优先级 ( 从低到高 ) ：</p>\n<p>application.yaml （忽略）</p>\n<h4>application.yml</h4>\n<p>application.properties</p>\n<p>java 系统属性（ -Dxxx=xxx ）</p>\n<p>命令行参数（ --xxx=xxx ）</p>\n<h4>singleton</h4>\n<p>容器内同名称的 bean 只有一个实例（单例）（默认）</p>\n<h4>prototype</h4>\n<p>每次使用该 bean 时会创建新的实例（非单例）</p>\n<h4>request</h4>\n<p>每个请求范围内会创建新的实例（ web 环境中，了解）</p>\n<h4>而且你就算把源码改了，下次 Maven 重新下载依赖，也没了。</h4>\n<p>所以 Spring 提供了：</p>\n<p>SpringBoot 框架之所以使用起来更简单更快捷，是因为 SpringBoot 框架底层提供了两个非常重要的功能：一个是起步依赖，一个是自动配置。</p>\n<p>通过 SpringBoot 所提供的起步依赖，就可以大大的简化 pom 文件当中依赖的配置，从而解决了 Spring 框架当中依赖配置繁琐的问题。</p>\n<p>通过自动配置的功能就可以大大的简化框架在使用时 bean 的声明以及 bean 的配置。我们只需要引入程序开发时所需要的起步依赖，项目开发时所用到常见的配置都已经有了，我们直接使用就可以了。</p>\n<p>假设你想开发一个 Web 项目。</p>\n<h4>Spring Boot 想：</h4>\n<p>既然你开发 Web 项目经常需要这些东西，那我给你打包成一个 “ 套餐 ” 。</p>\n<h4>于是出现：</h4>\n<p>Spring Boot Starter例如：</p>\n<pre class=\"note-code\">&lt;dependency&gt; &lt;groupId&gt;org.springframework.boot&lt;/groupId&gt; &lt;artifactId&gt;spring-boot-starter-web&lt;/artifactId&gt; &lt;/dependency&gt;</pre>\n<h4>spring-boot-starter-web</h4>\n<p>它会帮你把开发 Web 项目常用的一堆依赖带进来。</p>\n<h4>三、这里的 “ 起步依赖 ” 到底是什么？</h4>\n<p>起步依赖 = 帮你把依赖一次性配好这个名字非常容易让人迷糊。</p>\n<h4>你可以直接理解成：</h4>\n<p>针对某一种开发场景， Spring Boot 给你准备好的依赖套餐。</p>\n<p>例如：</p>\n<p>spring-boot-starter-web</p>\n<h4>就是：</h4>\n<p>我要开发 Web 。</p>\n<p>spring-boot-starter-test</p>\n<h4>就是：</h4>\n<p>我要进行测试。</p>\n<p>spring-boot-starter-data-jpa</p>\n<h4>就是：</h4>\n<p>我要使用 Spring Data JPA 。</p>\n<p>spring-boot-starter-data-redis</p>\n<h4>就是：</h4>\n<p>我要使用 Redis 。</p>\n<h4>所以：</h4>\n<p>开发需求 ↓ 选择对应 starter ↓ 一次性获得相关依赖</p>\n<h4>这就是第一句话：</h4>\n<p>通过 Spring Boot 提供的起步依赖，简化 pom.xml 中的依赖配置。</p>\n<p>起步依赖的原理就是 Maven 的依赖传递。</p>\n<p>在 SpringBoot 给我们提供的这些起步依赖当中，已提供了当前程序开发所需要的所有的常见依赖 ( 官网地址： https://docs.spring.io/spring-boot/docs/2.7.7/reference/htmlsingle/</p>\n<p>#using.build-systems.starters) 。</p>\n<p>解析自动配置的原理就是分析在 SpringBoot 项目当中，我们引入对应的依赖之后，是如何将依赖 jar 包当中所提供的 bean 以及配置类直接加载到当前项目的 SpringIOC 容器当中的。</p>\n<p>引入进来的第三方依赖当中的 bean 以及配置类为什么没有生效？</p>\n<p>原因在我们之前讲解 IOC 的时候有提到过，在类上添加 @Component 注解来声明 bean 对象时，还需要保证 @Component 注解能被 Spring 的组件扫描到。</p>\n<p>SpringBoot 项目中的 @SpringBootApplication 注解，具有包扫描的作用，但是它只会扫描启动类所在的当前包以及子包。</p>\n<p>当前包： com.itheima ， 第三方依赖中提供的包： com.example （扫描不到）那么如何解决以上问题的呢？</p>\n<h4>结论： SpringBoot 中并没有采用以上这种方案。</h4>\n<h4>自动配置源码小结</h4>\n<p>自动配置原理源码入口就是 @SpringBootApplication 注解，在这个注解中封装了 3 个注解，分别是：</p>\n<pre class=\"note-code\">@SpringBootConfiguration</pre>\n<h4>autoconfigure 模块（自动配置）</h4>\n<p>将来在项目当中进行相关功能开发时，只需要引入一个起步依赖就可以了，因为它会将 autoconfigure 自动配置的依赖给传递下来。</p>\n<p>SpringBoot 官方 starter 命名： spring-boot-starter-xxxx</p>\n<p>第三组织提供的 starter 命名： xxxx-spring-boot-starterM aven 高级内容</p>\n<h4>如果不分模块的话：</h4>\n<p>总结起来，主要两点问题：不方便项目的维护和管理、项目中的通用组件难以复用。</p>\n<p>所有的业务代码是不是都写在这一个 Java 项目当中。随着这个项目的业务扩张，项目当中的业务功能可能会越来越多。</p>\n<p>假如在我们的项目当中，我们自己定义了一些通用的工具类以及通用的组件，而公司还有其他的项目组，其他项目组也想使用我们所封装的这些组件和工具类，其实是非常不方便</p>\n<p>的。因为 Java 项目当中包含了当前项目的所有业务代码，所以就造成了这里面所封装的一些组件会难以复用。</p>\n<h4>分模块设计与开发</h4>\n<p>将项目当中的实体类、工具类以及我们定义的通用的组件都单独的抽取到一个模块当中。</p>\n<p>如果当前这个模块，比如订单模块需要用到这些实体类以及工具类或者这些通用组件，此时直接在订单模块当中引入工具类的坐标就可以了。这样我们就将一个项目拆分成了若干个模块儿，这就是分模块儿设计。</p>\n<p>将项目按照功能 / 结构拆分成若干个子模块，方便项目的管理维护、拓展，也方便模块键的相互调用、资源共享。</p>\n<p>策略一：按照功能模块拆分，比如：公共组件、商品模块、搜索模块、购物车模块、订单模块等。</p>\n<p>策略二：按层拆分，比如：公共组件、实体类、控制层、业务层、数据访问层。</p>\n<p>策略三：按照功能模块 + 层拆分。</p>\n<figure class=\"note-figure\"><img src=\"/notes-media/java-web-064.png\" alt=\"Spring Boot 起步依赖与自动配置 笔记配图\" loading=\"lazy\" /></figure>\n<p>分模块开发是根据模块功能进行设计在进行编码 而不会先将工程开发完再进行拆分</p>\n<p>什么是分模块设计： 将项目按照功能拆分成若干个子模块</p>\n<p>为什么要分模块设计： 方便项目的管理维护、扩展，也方便模块间的相互调用，资源共享</p>\n<p>注意事项： 分模块设计需要先针对模块功能进行设计，再进行编码。不会先将工程开发完毕，然后进行拆分</p>\n<p>如果是做一个大型的项目，模块当中重复的依赖可能会很多很多。如果每一个 Maven 模块里面，我们都来单独的配置一次，功能虽然能实现，但是配置是比较 繁琐 的 。</p>\n<h4>继承</h4>\n<p>概念：继承描述的是两个工程间的关系，与 java 中的继承相似，子工程可以继承父工程中的配置信息，常见于依赖关系的继承。</p>\n<p>作用：简化依赖配置、统一管理依赖</p>\n<h4>实现：</h4>\n<pre class=\"note-code\">&lt;parent&gt; &lt;groupId&gt;...&lt;/groupId&gt; &lt;artifactId&gt;...&lt;/artifactId&gt; &lt;version&gt;...&lt;/version&gt; &lt;relativePath&gt;....&lt;/relativePath&gt; &lt;/parent&gt;</pre>\n<figure class=\"note-figure\"><img src=\"/notes-media/java-web-065.png\" alt=\"Spring Boot 起步依赖与自动配置 笔记配图\" loading=\"lazy\" /></figure>\n<h4>Maven 打包方式：</h4>\n<p>jar ：普通模块打包， springboot 项目基本都是 jar 包（内嵌 tomcat 运行）</p>\n<p>war ：普通 web 程序打包，需要部署在外部的 tomcat 服务器中运行</p>\n<p>pom ：父工程或聚合工程，该模块不写代码，仅进行依赖管理</p>\n<p>创建 maven 模块 tlias-parent ，该工程为父工程，设置打包方式 pom( 默认 jar) 。</p>\n<p>在子工程中，配置了继承关系之后，坐标中的 groupId 是可以省略的，因为会自动继承父工程的 。</p>\n<p>relativePath 指定父工程的 pom 文件的相对位置（如果不指定，将从本地仓库 / 远程仓库查找该工程）。</p>\n<p>../ 代表的上一级目录</p>\n<h4>版本锁定</h4>\n<p>如果项目中各个模块中都公共的这部分依赖，我们可以直接定义在父工程中，从而简化子工程的配置。 然而在项目开发中，还有一部分依赖，并不是各个模块都共有的，可能只是</p>\n<p>其中的一小部分模块中使用到了这个依赖。</p>\n<p>假如说，我们项目要升级，要使用到 jwt 最新版本 0.9.2 中的一个新功能，那此时需要将依赖的版本升级到 0.9.2 ，那此时该怎么做呢 ？</p>\n<p>第一步：去找当前项目中所有的模块的 pom.xml 配置文件，看哪些模块用到了 jwt 的依赖。</p>\n<h4>第二步：找到这个依赖之后，将其版本 version ，更换为 0.9.2 。</h4>\n<p>问题：如果项目拆分的模块比较多，每一次更换版本，我们都得找到这个项目中的每一个模块，一个一个的更改。 很容易就会出现，遗漏掉一个模块，忘记更换版本的情况。</p>\n<p>那我们又该如何来解决这个问题，如何来统一管理各个依赖的版本呢？</p>\n<p>答案： Maven 的版本锁定功能。</p>\n<p>在 maven 中，可以在父工程的 pom 文件中通过 &lt;dependencyManagement&gt; 来统一管理依赖版本。</p>\n<p>在父工程中所配置的 &lt;dependencyManagement&gt; 只能统一管理依赖版本，并不会将这个依赖直接引入进来。 这点和 &lt;dependencies&gt; 是不同的。</p>\n<p>子工程要使用这个依赖，还是</p>\n<h4>属性配置</h4>\n<p>我们也可以通过自定义属性及属性引用的形式，在父工程中将依赖的版本号进行集中管理维护。</p>\n<p>需要引入的，只是此时就无需指定 &lt;version&gt; 版本号了，父工程统一管理。变更依赖版本，只需在父工程中统一变更。</p>\n<h4>1). 自定义属性</h4>\n<pre class=\"note-code\">&lt;properties&gt; &lt;lombok.version&gt;1.18.34&lt;/lombok.version&gt; &lt;/properties&gt;</pre>\n<h4>2). 引用属性</h4>\n<pre class=\"note-code\">&lt;dependency&gt; &lt;groupId&gt;org.projectlombok&lt;/groupId&gt; &lt;artifactId&gt;lombok&lt;/artifactId&gt; &lt;version&gt;${lombok.version}&lt;/version&gt; &lt;/dependency&gt;</pre>\n<p>面试题： &lt;dependencyManagement&gt; 与 &lt;dependencies&gt; 的区别是什么 ?</p>\n<pre class=\"note-code\">&lt;dependencies&gt; 是直接依赖，在父工程配置了依赖，子工程会直接继承下来。</pre>\n<pre class=\"note-code\">&lt;dependencyManagement&gt; 是统一管理依赖版本，不会直接依赖，还需要在子工程中引入所需依赖 ( 无需指定版本 )</pre>\n<p>聚合： 将多个模块组织成一个整体，同时进行项目的构建。</p>\n<p>聚合工程： 一个不具有业务功能的 “ 空 ” 工程（有且仅有一个 pom 文件） 【 PS ：一般来说，继承关系中的父工程与聚合关系中的聚合工程是同一个】</p>\n<p>作用： 快速构建项目（无需根据依赖关系手动构建，直接在聚合工程上构建即可）</p>\n<figure class=\"note-figure\"><img src=\"/notes-media/java-web-066.png\" alt=\"Spring Boot 起步依赖与自动配置 笔记配图\" loading=\"lazy\" /></figure>\n<pre class=\"note-code\">&lt;!-- 聚合其他模块 --&gt; &lt;modules&gt; &lt;module&gt;../tlias-pojo&lt;/module&gt; &lt;module&gt;../tlias-utils&lt;/module&gt; &lt;module&gt;../tlias-web-management&lt;/module&gt; &lt;/modules&gt;</pre>\n<p>在打包的过程中 maven 会自动处理这个不同继承与聚合对比</p>\n<h4>聚合用于快速构建项目</h4>\n<p>继承用于简化依赖配置、统一管理依赖</p>\n<h4>相同点：</h4>\n<p>聚合与继承的 pom.xml 文件打包方式均为 pom ，通常将两种关系制作到同一个 pom 文件中</p>\n<p>聚合与继承均属于 设计型模块，并无实际的模块内容</p>\n<h4>不同点：</h4>\n<p>聚合是在 聚合工程中配置关系 ，聚合可以感知到参与聚合的模块有哪些</p>\n<p>继承是在 子模块中配置关系，父模块无法感知哪些子模块继承了自己</p>\n<p>的依赖关系的，所有项目都依赖 parent 工程，所以第一个打包， management 依赖 pojo 和 utils 所以最后打包</p>\n<p>maven 的私服就出场了，私服其实就是架设在公司局域网内部的一台服务器，就是一种特殊的远程仓库。</p>\n<figure class=\"note-figure\"><img src=\"/notes-media/java-web-067.png\" alt=\"Spring Boot 起步依赖与自动配置 笔记配图\" loading=\"lazy\" /></figure>\n<p>依赖查找顺序：本地仓库私服仓库</p>\n<h4>第一步配置：在 maven 的配置文件中配置访问私服的用户名、密码。</h4>\n<p>第二步配置：在 maven 的配置文件中配置连接私服的地址 (url 地址 ) 。</p>\n<p>第三步配置：在项目的 pom.xml 文件中配置上传资源的位置 (url 地址 ) 。</p>\n<p>配置好了上述三步之后，要上传资源到私服仓库，就执行执行 maven 生命周期： deploy</p>\n<h4>私服仓库说明：</h4>\n<p>RELEASE ：存储自己开发的 RELEASE 发布版本的资源。</p>\n<p>SNAPSHOT ：存储自己开发的 SNAPSHOT 发布版本的资源。</p>\n<p>Central ：存储的是从中央仓库下载下来的依赖。</p>\n<h4>项目版本说明：</h4>\n<p>RELEASE( 发布版本 ) ：功能趋于稳定、当前更新停止，可以用于发行的版本，存储在私服中的 RELEASE 仓库中。</p>\n<p>SNAPSHOT( 快照版本 ) ：功能不稳定、尚处于开发中的版本，即快照版本，存储在私服的 SNAPSHOT 仓库中。</p>\n<p>1. 设置私服的访问用户名 / 密码（在自己 maven 安装目录下的 conf/settings.xml 中的 servers 中配置）</p>\n<pre class=\"note-code\">&lt;server&gt; &lt;id&gt;maven-releases&lt;/id&gt; &lt;username&gt;admin&lt;/username&gt; &lt;password&gt;*&lt;/password&gt; &lt;/server&gt; &lt;server&gt; &lt;id&gt;maven-snapshots&lt;/id&gt; &lt;username&gt;admin&lt;/username&gt; &lt;password&gt;*&lt;/password&gt; &lt;/server&gt;</pre>\n<p>2. 设置私服依赖下载的仓库组地址（在自己 maven 安装目录下的 conf/settings.xml 中的 mirrors 中配置）</p>\n<pre class=\"note-code\">&lt;mirror&gt; &lt;id&gt;maven-public&lt;/id&gt; &lt;mirrorOf&gt;*&lt;/mirrorOf&gt; &lt;url&gt;http://localhost:8081/repository/maven-public/&lt;/url&gt; &lt;/mirror&gt;</pre>\n<p>3. 设置私服依赖下载的仓库组地址（在自己 maven 安装目录下的 conf/settings.xml 中的 profiles 中配置）</p>\n<pre class=\"note-code\">&lt;profile&gt; &lt;id&gt;allow-snapshots&lt;/id&gt; &lt;activation&gt; &lt;activeByDefault&gt;true&lt;/activeByDefault&gt; &lt;/activation&gt; &lt;repositories&gt; &lt;repository&gt; &lt;id&gt;maven-public&lt;/id&gt; &lt;url&gt;http://localhost:8081/repository/maven-public/&lt;/url&gt; &lt;releases&gt; &lt;enabled&gt;true&lt;/enabled&gt; &lt;/releases&gt; &lt;snapshots&gt; &lt;enabled&gt;true&lt;/enabled&gt; &lt;/snapshots&gt; &lt;/repository&gt; &lt;/repositories&gt; &lt;/profile&gt;</pre>\n<p>4. IDEA 的 maven 工程的 pom 文件中配置上传（发布）地址 ( 直接在 tlias-parent 中配置发布地址 )</p>\n<pre class=\"note-code\">&lt;distributionManagement&gt; &lt;!-- release 版本的发布地址 --&gt; &lt;repository&gt; &lt;id&gt;maven-releases&lt;/id&gt; &lt;url&gt;http://localhost:8081/repository/maven-releases/&lt;/url&gt; &lt;/repository&gt; &lt;!-- snapshot 版本的发布地址 --&gt; &lt;snapshotRepository&gt; &lt;id&gt;maven-snapshots&lt;/id&gt; &lt;url&gt;http://localhost:8081/repository/maven-snapshots/&lt;/url&gt; &lt;/snapshotRepository&gt; &lt;/distributionManagement&gt;</pre>\n<p>配置完成之后，我们就可以在 tlias-parent 中执行 deploy 生命周期</p>\n<figure class=\"note-figure\"><img src=\"/notes-media/java-web-068.png\" alt=\"Spring Boot 起步依赖与自动配置 笔记配图\" loading=\"lazy\" /></figure>\n<pre class=\"note-code\">Npm ode Package manager NodeJS 软件包管理器</pre>\n<figure class=\"note-figure\"><img src=\"/notes-media/java-web-069.png\" alt=\"Spring Boot 起步依赖与自动配置 笔记配图\" loading=\"lazy\" /></figure>\n<h4>项目创建</h4>\n<p>创建一个工程化的 Vue 项目，执行命令： npm create vue@3.3.4</p>\n<figure class=\"note-figure\"><img src=\"/notes-media/java-web-070.png\" alt=\"Spring Boot 起步依赖与自动配置 笔记配图\" loading=\"lazy\" /></figure>\n<h4>详细步骤说明：</h4>\n<p>Project name ： ------------------ 》项目名称，默认值： vue-project ，可输入想要的项目名称。</p>\n<p>Add TypeScript? ---------------- 》是否加入 TypeScript 组件？默认值： No 。</p>\n<p>Add JSX Support? -------------- 》是否加入 JSX 支持？默认值： No 。</p>\n<p>Add Vue Router... -------------- 》是否为单页应用程序开发添加 Vue Router 路由管理组件？默认值： No 。</p>\n<p>Add Pinia ... ---------------------- 》是否添加 Pinia 组件来进行状态管理？默认值： No 。</p>\n<p>Add Vitest ... --------------------- 》是否添加 Vitest 来进行单元测试？默认值： No 。</p>\n<p>Add an End-to-End ... ----------- 》是否添加端到端测试？默认值 No 。</p>\n<p>Add ESLint for code quality? --- 》是否添加 ESLint 来进行代码质量检查？默认值： No 。</p>\n<p>提示： 执行上述指令，将会安装并执行 create-vue ，它是 Vue 官方的项目脚手架工具</p>\n<p>项目创建完成以后，进入 vue-project01 项目目录，执行命令安装当前项目的依赖： npm install</p>\n<p>这是我们创建的第一个项目结构，接下来呢，我们来介绍一下这个项目的结构。如图所示：</p>\n<figure class=\"note-figure\"><img src=\"/notes-media/java-web-071.png\" alt=\"Spring Boot 起步依赖与自动配置 笔记配图\" loading=\"lazy\" /></figure>\n<p>在上述的目录中，我们以后操作的最多的目录，就是 src 目录，因为我们需要在这个目录下来编写前端代码。</p>\n<h4>方式一：命令行</h4>\n<p>启动项目，我们可以在命令行中执行命令： npm run dev ，就可以启动 Vue 项目了。</p>\n<figure class=\"note-figure\"><img src=\"/notes-media/java-web-072.png\" alt=\"Spring Boot 起步依赖与自动配置 笔记配图\" loading=\"lazy\" /></figure>\n<h4>方式二： Vscode 图形化界面</h4>\n<p>点击 NPM 脚本中的 dev 后的运行按钮，就可以启动项目。</p>\n<figure class=\"note-figure\"><img src=\"/notes-media/java-web-073.png\" alt=\"Spring Boot 起步依赖与自动配置 笔记配图\" loading=\"lazy\" /></figure>\n<p>启动起来之后，我们就可以访问前端 Vue 项目了，访问路径： http://localhost:5173</p>\n<p>createApp 创建 Vue 应用实例</p>\n<h4>A pp.vue 根组件</h4>\n<p>其中 *.vue 是 Vue 项目中的组件文件，在 Vue 项目中也称为单文件组件（ SFC ， Single-File Components ）。 Vue 的单文件组件会将一个组件的逻辑 (JS) ，模板 (HTML) 和样式 (CSS) 封装在同一个文件里（ *.vue ）</p>\n<figure class=\"note-figure\"><img src=\"/notes-media/java-web-074.png\" alt=\"Spring Boot 起步依赖与自动配置 笔记配图\" loading=\"lazy\" /></figure>\n<p>Vue 的组件有两种不同的风格： 组合式 API 和 选项式 API</p>\n<p>组合式 API ： 是 Vue3 提供的一种基于函数的组件编写方式</p>\n<h4>代码形式如下：</h4>\n<pre class=\"note-code\">&lt;script setup&gt; import { ref, onMounted } from 'vue'; const count = ref(0); // 声明响应式变量</pre>\n<pre class=\"note-code\">function increment(){ // 声明函数 count.value++; }</pre>\n<pre class=\"note-code\">onMounted(() =&gt; { // 声明钩子函数 console.log('Vue Mounted....'); }) &lt;/script&gt;</pre>\n<pre class=\"note-code\">&lt;template&gt; &lt;input type=&quot;button&quot; @click=&quot;increment&quot;&gt; Api Demo1 Count : {{ count }} &lt;/template&gt;</pre>\n<pre class=\"note-code\">&lt;style scoped&gt; &lt;/style&gt;</pre>\n<p>setup ：是一个标识，告诉 Vue 需要进行一些处理，让我们可以更简洁的使用组合式 API 。</p>\n<p>ref() ：接收一个内部值，返回一个响应式的 ref 对象，此对象只有一个指向内部值的属性 value 。</p>\n<p>onMounted() ：在组合式 API 中的钩子方法，注册一个回调函数，在组件挂载完成后执行。</p>\n<h4>选项式 API</h4>\n<p>选项式 API ： 可以用包含多个选项的对象来描述组件的逻辑，如： data ， methods ， mounted 等。选项定义的属性都会暴露在函数内部的 this 上，它会指向当前的组件实例。</p>\n<p>组件从创建 → 渲染到页面 → 更新 → 销毁，整个生命周期里， Vue 自动在各个关键节点触发的函数，就叫钩子函数。 你可以 “ 钩住 ” 这个时机，写自己的业务代码。</p>\n<pre class=\"note-code\">&lt;script&gt; export default{ data() { return { count: 0 } }, methods: { increment: function(){ this.count++ } }, mounted() { console.log('vue mounted.....'); } } &lt;/script&gt;</pre>\n<pre class=\"note-code\">&lt;/style&gt;</pre>\n<p>在 Vue 中的组合式 API 使用时，是没有 this 对象的， this 对象是 undefined 。</p>\n<h4>响应式数据 ：一旦这个数据更新 视图也会跟着更新</h4>\n<p>当 &lt;style&gt; 标签带有 scoped 的时候，它的 CSS 只会影响当前组件的元素</p>\n<h4>默认导出 vs 命名导出</h4>\n<p>// ✅ 正确（你现在的写法） import ApiDemo from './views/ApiDemo.vue';</p>\n<pre class=\"note-code\">// ❌ 加花括号就报错 import { ApiDemo } from './views/ApiDemo.vue';</pre>\n<pre class=\"note-code\">大括号 {} 是用来导入 命名导出 ；不带大括号是导入 默认导出 。 单个 .vue 单文件组件， 永远是默认导出 。</pre>\n<h4>两种导出方式对比</h4>\n<p>1. 默认导出（ .vue 组件都是这个）</p>\n<h4>ApiDemo.vue 底层等价于：</h4>\n<p>// 组件文件内部 export default 组件对象</p>\n<h4>导入语法： 没有大括号</h4>\n<pre class=\"note-code\">import ApiDemo from './views/ApiDemo.vue'</pre>\n<p>名字你可以随便起，比如写成 import Demo from './views/ApiDemo.vue' 也能跑。</p>\n<h4>2. 命名导出（才需要大括号）</h4>\n<pre class=\"note-code\">// 模块内部 export const ApiDemo = {...}</pre>\n<pre class=\"note-code\">导入必须带 {} ，名字必须和导出名字一模一样：</pre>\n<pre class=\"note-code\">import { ApiDemo } from './xxx.js'</pre>\n<p>一个文件可以有多个命名导出； 但是 .vue 单文件组件没有这个 。</p>\n<h4>一句话口诀</h4>\n<pre class=\"note-code\">export default → import 不带大括号 ，名字随便写</pre>\n<pre class=\"note-code\">export const xxx → import 带大括号 ，名字必须严格匹配容易踩坑的 Vue 场景什么时候需要大括号？</pre>\n<pre class=\"note-code\">// 从 vue 库导入多个函数，属于命名导出，必须 {} import { ref, onMounted } from 'vue'</pre>\n<pre class=\"note-code\">vue 库里面 ref 、 onMounted 是一个个单独命名导出，所以加 {} 。 组件文件是 export default ， 不能加大括号 。</pre>\n<h4>额外报错现象</h4>\n<pre class=\"note-code\">如果你写 import {ApiDemo} ，浏览器 / 控制台一般会提示：</pre>\n<p>ApiDemo is not exported by ./views/ApiDemo.vue 意思：这个文件 没有叫 ApiDemo 的命名导出 ，只有默认导出。</p>\n<figure class=\"note-figure\"><img src=\"/notes-media/java-web-075.png\" alt=\"Spring Boot 起步依赖与自动配置 笔记配图\" loading=\"lazy\" /></figure>"
      },
      {
        "id": "ioc-bean",
        "title": "IOC / Bean / 注解配置",
        "summary": "IOC / DI、Bean 作用域、@Import 与条件装配。",
        "html": "<h4>八、 Spring 核心： IOC 与 DI</h4>\n<h4>1. 基本概念</h4>\n<p>控制反转 (IOC - Inversion of Control) ：将对象的创建控制权由程序代码自身 (new) 转移到 Spring 容器中。被容器管理的对象称为 Bean 。</p>\n<p>依赖注入 (DI - Dependency Injection) ： Spring 容器自动为程序提供所需的对象。</p>\n<h4>2. IOC 常用组件注解</h4>\n<p>声明 Bean 的衍生注解（作用与 @Component 一致）：</p>\n<pre class=\"note-code\">@Component ：通用 Bean 声明。</pre>\n<h4>当一个接口有多个实现类 Bean 时 ：</h4>\n<p>1. 在 Preferred 实现类上加 @Primary 设置为默认。</p>\n<p>2. 使用 @Autowired + @Qualifier(&quot;beanName&quot;) 指定 Bean 名称。</p>\n<p>3. 使用 @Resource(name = &quot;beanName&quot;) 指定。</p>\n<p>九、 数据库与 MyBatis 持久层</p>\n<h4>1. 数据库连接池</h4>\n<p>优势 ：预先创建并复用数据库连接，减少频繁创建销毁带来的性能损耗；管理最大连接数、空闲连接监控，防止连接泄露。</p>\n<h4>2. MyBatis 基础</h4>\n<p>预编译 SQL ：使用 # 占位符 ? ，防止 SQL 注入。</p>\n<h4>注解方式操作 SQL ：</h4>\n<pre class=\"note-code\">@Select, @Insert, @Update, @Delete 。</pre>\n<p>使用 @Param(&quot; 名称 &quot;) 给参数取别名。</p>\n<h4>映射规范 ：</h4>\n<p>Mapper 接口与 XML 文件需同包同名。</p>\n<p>XML 中的 id 属性须与接口方法名一致，返回类型一致。</p>\n<h4>属性与字段名不一致问题 ：</h4>\n<p>实体类属性名与数据库字段名不匹配时无法自动封装。</p>\n<p>解决办法：开启驼峰自动映射；或使用 SQL 别名；或通过 @Results / @Result 手动指定映射。</p>\n<h4>可以借助 Spring 中的 @Scope 注解来进行配置作用域</h4>\n<figure class=\"note-figure\"><img src=\"/notes-media/java-web-061.png\" alt=\"IOC / Bean / 注解配置 笔记配图\" loading=\"lazy\" /></figure>\n<p>默认 singleton 的 bean ，在容器启动时被创建，可以使用 @Lazy 注解来延迟初始化（延迟到第一次使用时）</p>\n<p>prototype 的 bean ，每一次使用该 bean 的时候都会创建一个新的实例。</p>\n<p>实际开发当中，绝大部分的 bean 是单例的，也就是说绝大部分 bean 不需要配置 scope 属性。</p>\n<p>Spring 对 prototype Bean 负责创建和初始化，但通常不负责它的销毁</p>\n<p>在我们项目开发当中，还有一种情况就是这个类它不是我们自己编写的，而是我们引入的第三方依赖当中提供的，那么此时我们是无法使用 @Component 及其衍生注解来声明 bean 的，此时就需要使用 @Bean 注解来声明 bean 了。</p>\n<p>若要管理的第三方 bean 对象，建议对这些 bean 进行集中分类配置，可以通过 @Configuration 注解声明一个配置类。</p>\n<p>你项目引入了一个第三方依赖，里面有一个：RedisClient这个类不是你写的。</p>\n<h4>你不能跑到第三方源码里面改成：</h4>\n<pre class=\"note-code\">@Component public class RedisClient { }</pre>\n<h4>因为：</h4>\n<p>这个类的源码不是你的。</p>\n<pre class=\"note-code\">@Bean</pre>\n<h4>意思就是：</h4>\n<p>这个对象虽然不是你写的，但是你告诉 Spring ： “ 请你把我创建出来的这个对象交给你管理。 ”</p>\n<h4>public class Dog {</h4>\n<pre class=\"note-code\">public void bark() { System.out.println(&quot; 汪汪汪 &quot;); } }注意：</pre>\n<p>假设这个 Dog 是第三方依赖提供的，我们不能修改它。</p>\n<h4>那么我们创建一个配置类：</h4>\n<pre class=\"note-code\">@Configuration public class AppConfig {</pre>\n<pre class=\"note-code\">@Bean public Dog dog() { return new Dog(); } }</pre>\n<h4>这里最关键的是：</h4>\n<h4>你可以把它翻译成人话：</h4>\n<p>Spring ，你帮我调用 new Dog() 创建一个 Dog 对象，然后把这个对象放进你的 Bean 容器里面。</p>\n<h4>于是：</h4>\n<pre class=\"note-code\">@Configuration ↓ AppConfig ↓ @Bean ↓ new Dog() ↓ Dog 对象 ↓ Spring Bean 容器</pre>\n<h4>四、然后我们就可以注入了</h4>\n<p>比如：</p>\n<pre class=\"note-code\">@Service public class UserService {</pre>\n<pre class=\"note-code\">@Autowired private Dog dog;</pre>\n<pre class=\"note-code\">public void test() { dog.bark(); } }</pre>\n<p>Spring 会自动把刚才的 Dog Bean 注入进来。</p>\n<h4>于是：</h4>\n<p>dog.bark();就可以正常调用。</p>\n<p>Spring Core Spring Beans Spring Context Spring MVC Servlet Jackson Tomcat 日志相关 ...</p>\n<p>而且这些东西之间还有 版本兼容、依赖关系 。</p>\n<p>比如你想使用 Spring MVC ，可能需要在 pom.xml 里写一堆：</p>\n<pre class=\"note-code\">&lt;dependency&gt; &lt;groupId&gt;org.springframework&lt;/groupId&gt; &lt;artifactId&gt;spring-context&lt;/artifactId&gt; &lt;version&gt;...&lt;/version&gt; &lt;/dependency&gt;</pre>\n<p>非常麻烦。</p>\n<h4>方案 1 ： @ComponentScan 组件扫描</h4>\n<p>方案 2 ： @Import 导入（使用 @Import 导入的类会被 Spring 加载到 IOC 容器中）</p>\n<pre class=\"note-code\">@ComponentScan 组件扫描</pre>\n<pre class=\"note-code\">@SpringBootApplication @ComponentScan({&quot;com.itheima&quot;,&quot;com.example&quot;}) // 指定要扫描的包 public class SpringbootWebConfigApplication { public static void main(String[] args) { SpringApplication.run(SpringbootWebConfigApplication.class, args); } }重新执行测试方法，控制台日志输出：</pre>\n<figure class=\"note-figure\"><img src=\"/notes-media/java-web-062.png\" alt=\"IOC / Bean / 注解配置 笔记配图\" loading=\"lazy\" /></figure>\n<p>如果采用以上这种方式来完成自动配置，那我们进行项目开发时，当需要引入大量的第三方的依赖，就需要在启动类上配置 N 多要扫描的包，这种方式会很繁琐。而且这种大面积的扫描性能也比较低。</p>\n<p>缺点：使用繁琐</p>\n<h4>先记住核心：</h4>\n<pre class=\"note-code\">@Import = 告诉 Spring ：把某些东西加入 IoC 容器。</pre>\n<h4>普通情况下：</h4>\n<pre class=\"note-code\">@Component public class UserService { }</pre>\n<p>Spring 可以通过组件扫描发现它。</p>\n<p>但是如果一个类 没有 @Component ，或者你想 手动 / 动态决定导入什么 ，就可以使用 @Import 。</p>\n<h4>二、 @Import 四种常见使用方式</h4>\n<p>① 导入普通类</p>\n<h4>最简单：</h4>\n<pre class=\"note-code\">@Import(TokenParser.class) @SpringBootApplication public class Application { }</pre>\n<h4>相当于告诉 Spring ：</h4>\n<p>TokenParser ↓ 加入 IoC 容器 ↓ 成为 Bean</p>\n<h4>之后就可以：</h4>\n<pre class=\"note-code\">@Autowired private TokenParser tokenParser;</pre>\n<h4>记忆</h4>\n<p>直接 @Import( 类 .class) → 把这个类交给 Spring 管理。</p>\n<p>② 导入配置类例如：</p>\n<pre class=\"note-code\">@Configuration public class HeaderConfig {</pre>\n<pre class=\"note-code\">@Bean public HeaderParser headerParser() { return new HeaderParser(); }</pre>\n<pre class=\"note-code\">@Bean public HeaderGenerator headerGenerator() { return new HeaderGenerator(); } }</pre>\n<h4>然后：</h4>\n<pre class=\"note-code\">@Import(HeaderConfig.class) @SpringBootApplication public class Application { }</pre>\n<h4>Spring 加载：</h4>\n<pre class=\"note-code\">@Import(HeaderConfig.class) ↓ HeaderConfig ↓ 发现两个 @Bean ↙ ↘ HeaderParser HeaderGenerator ↓ ↓ 加入 IoC 容器</pre>\n<p>所以它不只是把 HeaderConfig 当成普通 Bean 。</p>\n<h4>而是：</h4>\n<p>把这个配置类加载进来，让里面的 @Bean 配置生效。</p>\n<h4>记忆</h4>\n<pre class=\"note-code\">@Import( 配置类 .class) → 导入整套配置。</pre>\n<p>③ 导入 ImportSelector这个就开始 “ 动态 ” 了。</p>\n<p>例如：</p>\n<p>public class MyImportSelector implements ImportSelector {</p>\n<pre class=\"note-code\">@Override public String[] selectImports(AnnotationMetadata metadata) { return new String[]{ &quot;com.example.UserService&quot;, &quot;com.example.OrderService&quot; }; } }</pre>\n<h4>然后：</h4>\n<pre class=\"note-code\">@Import(MyImportSelector.class) @SpringBootApplication public class Application { }</pre>\n<h4>Spring 发现：</h4>\n<pre class=\"note-code\">@Import(MyImportSelector.class) ↓ 创建 MyImportSelector ↓ 调用 selectImports() ↓ 得到： UserService OrderService ↓ 导入这些类 ↓ 成为 Bean</pre>\n<h4>它的特点就是：</h4>\n<p>不是我直接告诉 Spring 导入哪个类，而是让 Selector 帮我决定导入哪些类。</p>\n<h4>记忆</h4>\n<p>ImportSelector → 动态选择 “ 导入哪些类 ” 。</p>\n<p>④ 导入 ImportBeanDefinitionRegistrar这是更底层的一种方式。</p>\n<p>它不是简单返回几个类名，而是可以直接操作 Spring 的：</p>\n<p>BeanDefinitionRegistry例如：</p>\n<p>public class MyRegistrar implements ImportBeanDefinitionRegistrar {</p>\n<pre class=\"note-code\">@Override public void registerBeanDefinitions( AnnotationMetadata metadata, BeanDefinitionRegistry registry) {</pre>\n<p>BeanDefinition beanDefinition = BeanDefinitionBuilder .rootBeanDefinition(UserService.class) .getBeanDefinition();</p>\n<p>registry.registerBeanDefinition( &quot;userService&quot;, beanDefinition ); } }</p>\n<h4>然后：</h4>\n<pre class=\"note-code\">@Import(MyRegistrar.class)</pre>\n<h4>它的过程可以理解成：</h4>\n<pre class=\"note-code\">@Import(MyRegistrar.class) ↓ 执行 Registrar ↓ 直接操作 Spring 的 BeanDefinition ↓ 告诉 Spring ： “ 注册一个叫 userService 的 Bean”</pre>\n<p>所以它比 ImportSelector 更底层、更灵活。</p>\n<h4>记忆</h4>\n<p>ImportBeanDefinitionRegistrar → 直接操作 BeanDefinition ，动态注册 Bean 。</p>\n<h4>三、还有一个你刚刚问的： @EnableXxx</h4>\n<p>这个其实 不是第五种 @Import 。</p>\n<p>它是对前面 @Import 的一种 封装和应用 。</p>\n<p>例如第三方库写：</p>\n<pre class=\"note-code\">@Retention(RetentionPolicy.RUNTIME) @Target(ElementType.TYPE) @Import(MyImportSelector.class) public @interface EnableHeaderConfig { }</pre>\n<h4>你使用的时候只需要：</h4>\n<pre class=\"note-code\">@EnableHeaderConfig @SpringBootApplication public class Application { }</pre>\n<h4>实际发生的是：</h4>\n<pre class=\"note-code\">@EnableHeaderConfig ↓ 里面偷偷有 @Import(MyImportSelector.class) ↓ 执行 MyImportSelector ↓ selectImports() ↓ 导入需要的 Bean</pre>\n<h4>所以：</h4>\n<pre class=\"note-code\">@EnableXxx = 把一堆复杂的 Spring 配置 / 导入逻辑包装成一个简单的 “ 开启功能 ” 注解。</pre>\n<h4>这就是为什么 Spring 中经常看到：</h4>\n<pre class=\"note-code\">@EnableAsync @EnableScheduling @EnableTransactionManagement</pre>\n<pre class=\"note-code\">@EnableXXX</pre>\n<p>不用关心它背后到底 @Import 了什么。</p>\n<h4>四、最终总结表</h4>\n<p>方式</p>\n<h4>① 普通类</h4>\n<pre class=\"note-code\">@Import(A.class)直接把 A 加入 IoC</pre>\n<h4>② 配置类</h4>\n<pre class=\"note-code\">@Import(Config.class)</pre>\n<p>导入配置类，让里面的 @Bean 生效</p>\n<h4>③ ImportSelector</h4>\n<pre class=\"note-code\">@Import(MySelector.class)</pre>\n<h4>④ Registrar</h4>\n<p>直接操作 BeanDefinition ，动态注册 Bean</p>\n<pre class=\"note-code\">@EnableXxx</pre>\n<pre class=\"note-code\">@EnableHeaderConfig</pre>\n<p>对 @Import 等复杂逻辑进行封装</p>\n<pre class=\"note-code\">@ComponentScan</pre>\n<p>进行组件扫描（ SpringBoot 中默认扫描的是启动类所在的当前包及其子包）</p>\n<pre class=\"note-code\">@EnableAutoConfiguration</pre>\n<p>封装了 @Import 注解（ Import 注解中指定了一个 ImportSelector 接口的实现类） 在实现类重写的 selectImports() 方法，读取当前项目下所有依赖 jar 包中 META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports 两个文件里面定义的配置类（配置类中定义了 @Bean 注解标识的方法）。</p>\n<p>当 SpringBoot 程序启动时，就会加载配置文件当中所定义的配置类，并将这些配置类信息 ( 类的全限定名 ) 封装到 String 类型的数组中，最终通过 @Import 注解将这些配置类全部加载到 Spring 的 IOC 容器中，交给 IOC 容器管理。</p>\n<p>最后呢给大家抛出一个问题：在 META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports 文件中定义的配置类非常多，而且每个配置类中又可以定义很多的 bean ，那这些 bean 都会注册到 Spring 的 IOC 容器中吗？</p>\n<p>答案：并不是。 在声明 bean 对象时，上面有加一个以 @Conditional 开头的注解，这种注解的作用就是按照条件进行装配，只有满足条件之后，才会将 bean 注册到 Spring 的 IOC 容器中（下面会详细来讲解）</p>\n<figure class=\"note-figure\"><img src=\"/notes-media/java-web-063.png\" alt=\"IOC / Bean / 注解配置 笔记配图\" loading=\"lazy\" /></figure>\n<p>作用：按照一定的条件进行判断，在满足给定条件后才会注册对应的 bean 对象到 Spring 的 IOC 容器中。</p>\n<h4>位置：方法、类</h4>\n<pre class=\"note-code\">@Conditional 本身是一个父注解，派生出大量的子注解：</pre>\n<pre class=\"note-code\">@ConditionalOnClass ：判断环境中有对应字节码文件，才注册 bean 到 IOC 容器。</pre>\n<pre class=\"note-code\">@ConditionalOnMissingBean ：判断环境中没有对应的 bean( 类型或名称 ) ，才注册 bean 到 IOC 容器。</pre>\n<pre class=\"note-code\">@ConditionalOnProperty ：判断配置文件中有对应属性和值，才注册 bean 到 IOC 容器。</pre>\n<p>1. @ConditionalOnClass</p>\n<h4>存在指定类的字节码，才创建 Bean</h4>\n<pre class=\"note-code\">@Configuration public class DemoConfig { // 当 classpath 里面有 RedisTemplate 这个类（引入 spring-data-redis 依赖），才创建 bean @Bean @ConditionalOnClass(RedisTemplate.class) public RedisTemplate&lt;String,Object&gt; redisTemplate(){ return new RedisTemplate&lt;&gt;(); } }</pre>\n<p>2. @ConditionalOnMissingBean</p>\n<p>容器中没有该 Bean ，才创建；用户自己定义优先</p>\n<pre class=\"note-code\">@Bean @ConditionalOnMissingBean public UserService userService(){ return new UserService(); } // 如果 IOC 容器已经存在 UserService 类型 Bean ，这个 @Bean 就不会执行</pre>\n<p>3. @ConditionalOnProperty</p>\n<p>配置文件存在指定属性，并且值匹配，才创建 Bean application.yml</p>\n<h4>demo.enable=true</h4>\n<pre class=\"note-code\">@Bean // 前缀 demo ，属性名 enable ，匹配值 true 才生效 @ConditionalOnProperty(prefix = &quot;demo&quot;, name = &quot;enable&quot;, havingValue = &quot;true&quot;) public DemoBean demoBean(){ return new DemoBean(); }</pre>\n<pre class=\"note-code\">@SpringBootConfiguration 继承 @Configuration ，标记当前类为 配置类 ；可以在类内使用 @Bean 向 IOC 容器注册 Bean 。</pre>\n<pre class=\"note-code\">@ComponentScan 开启组件扫描， 默认扫描启动类所在包及其所有子包 ，自动识别 @Component 、 @Service 、 @Controller 、 @Repository 等注解的类，注册为 Bean 。</pre>\n<pre class=\"note-code\">@EnableAutoConfiguration 开启 SpringBoot 自动配置 。根据项目 classpath 下引入的依赖，自动加载对应的配置类，按需创建 Bean ，实现 “ 约定大于配置 ” 。底层读取自动配置文件，结合条件注解判断配置是否生效。</pre>\n<p>自动配置的核心就在 @SpringBootApplication 注解上， SpringBootApplication 这个注解底层包含了 3 个注解，分别是：</p>\n<pre class=\"note-code\">@EnableAutoConfiguration</pre>\n<pre class=\"note-code\">@EnableAutoConfiguration 这个注解才是自动配置的核心。</pre>\n<p>它封装了一个 @Import 注解， Import 注解里面指定了一个 ImportSelector 接口的实现类。</p>\n<p>在这个实现类中，重写了 ImportSelector 接口中的 selectImports() 方法。</p>\n<p>而 selectImports() 方法中会去读取两份配置文件，并将配置文件中定义的配置类做为 selectImports() 方法的返回值返回，返回值代表的就是需要将哪些类交给 Spring 的 IOC 容器进行管理。</p>\n<p>那么所有自动配置类的中声明的 bean 都会加载到 Spring 的 IOC 容器中吗 ? 其实并不会，因为这些配置类中在声明 bean 时，通常都会添加 @Conditional 开头的注解，这个注解就是进行条件装配。而 Spring 会根据 Conditional 注解有选择性的进行 bean 的创建。</p>\n<pre class=\"note-code\">@Enable 开头的注解底层，它就封装了一个注解 import 注解，它里面指定了一个类，是 ImportSelector 接口的实现类。在实现类当中，我们需要去实现 ImportSelector 接口当中的一个方法 selectImports 这个方法。这个方法的返回值代表的就是我需要将哪些类交给 spring 的 IOC 容器进行管理。</pre>\n<p>此时它会去读取两份配置文件，一份儿是 spring.factories ，另外一份儿是 autoConfiguration.imports 。而在 autoConfiguration.imports 这份儿文件当中，它就会去配置大量的自动配置的类。</p>\n<p>而前面我们也提到过这些所有的自动配置类当中，所有的 bean 都会加载到 spring 的 IOC 容器当中吗？其实并不会，因为这些配置类当中，在声明 bean 的时候，通常会加上这么一类 @Conditional 开头的注解。这个注解就是进行条件装配。所以 SpringBoot 非常的智能，它会根据 @Conditional 注解来进行条件装配。只有条件成立，它才会声明这个 bean ，才会将这个 bean 交给 IOC 容器管理。</p>\n<p>所谓 starter 指的就是 SpringBoot 当中的起步依赖。在 SpringBoot 当中已经给我们提供了很多的起步依赖了，我们为什么还需要自定义 starter 起步依赖？</p>\n<p>这是因为在实际的项目开发当中，我们可能会用到很多第三方的技术，并不是所有的第三方的技术官方都给我们提供了与 SpringBoot 整合的 starter 起步依赖，但是这些技术又非常的通用，在很多项目组当中都在使用。</p>\n<p>在自定义一个起步依赖 starter 的时候，按照规范需要定义两个模块：</p>\n<p>starter 模块（进行依赖管理 [ 把程序开发所需要的依赖都定义在 starter 起步依赖中 ] ）</p>"
      },
      {
        "id": "aop-tx",
        "title": "AOP 与事务管理",
        "summary": "AOP 核心概念、通知类型与 @Transactional 事务。",
        "html": "<h4>注解： @Transactional</h4>\n<p>作用： 就是在当前这个方法执行开始之前来开启事务，方法执行完毕之后提交事务。如果在这个方法执行的过程当中出现了异常，就会进行事务的回滚操作。</p>\n<h4>方法上：当前方法交给 spring 进行事务管理</h4>\n<p>类上：当前类中所有的方法都交由 spring 进行事务管理</p>\n<p>接口上：接口下所有的实现类当中所有的方法都交给 spring 进行事务管理</p>\n<p>默认情况下，只有出现 RuntimeException( 运行时异常 ) 才会回滚事务。</p>\n<p>rollbackFor 属性 指定遇到什么异常时回滚</p>\n<h4>事物的传播行为</h4>\n<p>一个事物方法调用另一个事物方法 这个方法是如何进行事务控制</p>\n<h4>propagation 属性</h4>\n<p>REQUIRED 需要事物 有就加入那个事物 没有则创建一个新事务</p>\n<p>REQUIRES_NEW 需要新事务，无论有无总是创建新事物</p>\n<h4>（不希望事物之间相互影响）</h4>\n<p>第一个 如果我在我的事物方法中引入一个日志事务方法 propagation=REQUIRED 那么如果我的事物方法回滚 就不会执行日志操作</p>\n<p>第二个 如果我在我的事务方法中引入一个日志事物方法 propagation=REQUIRES_NEW 那么如果我的事物方法回滚 内部的日志操作不受干扰仍然执行</p>\n<p>原子性（ Atomicity ） ：原子性是指事务包装的一组 sql 是一个不可分割的工作单元，事务中的操作要么全部成功，要么全部失败。</p>\n<p>一致性（ Consistency ） ：一个事务完成之后数据都必须处于一致性状态。</p>\n<p>如果事务成功的完成，那么数据库的所有变化将生效。</p>\n<p>如果事务执行出现错误，那么数据库的所有变化将会被回滚 ( 撤销 ) ，返回到原始状态。</p>\n<p>隔离性（ Isolation ） ：多个用户并发的访问数据库时，一个用户的事务不能被其他用户的事务干扰，多个并发的事务之间要相互隔离。</p>\n<p>一个事务的成功或者失败对于其他的事务是没有影响。</p>\n<p>持久性（ Durability ） ：一个事务一旦被提交或回滚，它对数据库的改变将是永久性的，哪怕数据库发生异常，重启之后数据亦然存在。</p>\n<p>上传文件的原始 form 表单，要求表单必须具备以下三点（上传文件页面三要素）：</p>\n<h4>表单必须有 file 域，用于选择要上传的文件</h4>\n<p>表单提交方式必须为 POST ：通常上传的文件会比较大，所以需要使用 POST 提交方式</p>\n<p>表单的编码类型 enctype 必须要设置为： multipart/form-data ：普通默认的编码格式是不适合传输大型的二进制数据的，所以在文件上传时，表单的编码格式必须设置为 multipart/form-data</p>\n<p>String getOriginalFilename(); // 获取原始文件名</p>\n<p>void transferTo(File dest); // 将接收的文件转存到磁盘文件中</p>\n<p>long getSize(); // 获取文件的大小，单位：字节</p>\n<p>byte[] getBytes(); // 获取文件内容的字节数组</p>\n<p>InputStream getInputStream(); // 获取接收到的文件内容的输入流</p>\n<h4>AOP 的优势主要体现在以下四个方面：</h4>\n<p>减少重复代码：不需要在业务方法中定义大量的重复性的代码，只需要将重复性的代码抽取到 AOP 程序中即可。</p>\n<p>代码无侵入：在基于 AOP 实现这些业务功能时，对原有的业务代码是没有任何侵入的，不需要修改任何的业务代码。</p>\n<p>提高开发效率</p>\n<h4>维护方便</h4>\n<p>常见的应用场景如下：记录系统的操作日志</p>\n<h4>权限控制</h4>\n<p>事务管理：我们前面所讲解的 Spring 事务管理，底层其实也是通过 AOP 来实现的，只要添加 @Transactional 注解之后， AOP 程序自动会在原始方法运行前先来开启事务，在原始方法运行完毕之后提交或回滚事务</p>\n<p>通过 SpringAOP 的快速入门，感受了一下 AOP 面向切面编程的开发方式。下面我们再来学习 AOP 当中涉及到的一些核心概念。</p>\n<p>连接点： JoinPoint ，可以被 AOP 控制的方法（暗含方法执行时的相关信息）</p>\n<p>连接点指的是可以被 aop 控制的方法。例如：入门程序当中所有的业务方法都是可以被 aop 控制的方法。</p>\n<p>在 SpringAOP 提供的 JoinPoint 当中，封装了连接点方法在执行时的相关信息。（后面会有具体的讲解）</p>\n<figure class=\"note-figure\"><img src=\"/notes-media/java-web-053.png\" alt=\"AOP 与事务管理 笔记配图\" loading=\"lazy\" /></figure>\n<p>通知： Advice ，指哪些重复的逻辑，也就是共性功能（最终体现为一个方法）</p>\n<p>在入门程序中是需要统计各个业务方法的执行耗时的，此时我们就需要在这些业务方法运行开始之前，先记录这个方法运行的开始时间，在每一个业务方法运行结束的时候，再来记录这个方法运行的结束时间。</p>\n<p>是在 AOP 面向切面编程当中，我们只需要将这部分重复的代码逻辑抽取出来单独定义。抽取出来的这一部分重复的逻辑，也就是共性的功能。</p>\n<figure class=\"note-figure\"><img src=\"/notes-media/java-web-054.png\" alt=\"AOP 与事务管理 笔记配图\" loading=\"lazy\" /></figure>\n<p>切入点： PointCut ，匹配连接点的条件，通知仅会在切入点方法执行时被应用。</p>\n<p>在通知当中，我们所定义的共性功能到底要应用在哪些方法上？此时就涉及到了切入点 pointcut 概念。切入点指的是匹配连接点的条件。通知仅会在切入点方法运行时才会被应用。</p>\n<p>在 aop 的开发当中，我们通常会通过一个切入点表达式来描述切入点 ( 后面会有详解 ) 。</p>\n<figure class=\"note-figure\"><img src=\"/notes-media/java-web-055.png\" alt=\"AOP 与事务管理 笔记配图\" loading=\"lazy\" /></figure>\n<p>假如：切入点表达式改为 DeptServiceImpl.list() ，此时就代表仅仅只有 list 这一个方法是切入点。只有 list() 方法在运行的时候才会应用通知。</p>\n<p>切面： Aspect ，描述通知与切入点的对应关系（通知 + 切入点）</p>\n<p>当通知和切入点结合在一起，就形成了一个切面。通过切面就能够描述当前 aop 程序需要针对于哪个原始方法，在什么时候执行什么样的操作。</p>\n<figure class=\"note-figure\"><img src=\"/notes-media/java-web-056.png\" alt=\"AOP 与事务管理 笔记配图\" loading=\"lazy\" /></figure>\n<p>而切面所在的类，称之为切面类（被 @Aspect 注解标识的类）。</p>\n<h4>目标对象： Target ，通知所应用的对象</h4>\n<p>目标对象指的就是通知所应用的对象，我们就称之为目标对象。</p>\n<figure class=\"note-figure\"><img src=\"/notes-media/java-web-057.png\" alt=\"AOP 与事务管理 笔记配图\" loading=\"lazy\" /></figure>\n<p>SpringAOP 旨在管理 bean 对象的过程中，主要通过底层的动态代理机制，对特定的方法进行编程 。</p>\n<p>Spring AOP 通知类型</p>\n<pre class=\"note-code\">@Around</pre>\n<p>环绕通知，此注解标注的通知方法在目标方法前、后都被执行</p>\n<pre class=\"note-code\">@Before</pre>\n<p>前置通知，此注解标注的通知方法在目标方法前被执行</p>\n<pre class=\"note-code\">@After</pre>\n<p>后置通知，此注解标注的通知方法在目标方法后被执行，无论是否有异常都会执行</p>\n<pre class=\"note-code\">@AfterReturning</pre>\n<p>返回后通知，此注解标注的通知方法在目标方法后被执行，有异常不会执行</p>\n<pre class=\"note-code\">@AfterThrowing</pre>\n<p>异常后通知，此注解标注的通知方法发生异常后执行</p>\n<figure class=\"note-figure\"><img src=\"/notes-media/java-web-058.png\" alt=\"AOP 与事务管理 笔记配图\" loading=\"lazy\" /></figure>\n<h4>抽取</h4>\n<p>Spring 提供了 @PointCut 注解，该注解的作用是将公共的切入点表达式抽取出来，需要用到时引用该切入点表达式即可。</p>\n<pre class=\"note-code\">@Slf4j @Component @Aspect public class MyAspect1 {</pre>\n<pre class=\"note-code\">// 切入点方法（公共的切入点表达式） @Pointcut(&quot;execution(* com.itheima.service.*.*(..))&quot;) private void pt(){}</pre>\n<pre class=\"note-code\">// 前置通知（引用切入点） @Before(&quot;pt()&quot;) public void before(JoinPoint joinPoint){ log.info(&quot;before ...&quot;);}</pre>\n<p>// 环绕通知 @Around(&quot;pt()&quot;) public Object around(ProceedingJoinPoint proceedingJoinPoint) throws Throwable { log.info(&quot;around before ...&quot;);</p>\n<p>// 调用目标对象的原始方法执行 Object result = proceedingJoinPoint.proceed(); // 原始方法在执行时：发生异常 // 后续代码不在执行</p>\n<p>log.info(&quot;around after ...&quot;); return result; }</p>\n<pre class=\"note-code\">// 后置通知 @After(&quot;pt()&quot;) public void after(JoinPoint joinPoint){ log.info(&quot;after ...&quot;); }</pre>\n<p>// 返回后通知（程序在正常执行的情况下，会执行的后置通知） @AfterReturning(&quot;pt()&quot;) public void afterReturning(JoinPoint joinPoint){ log.info(&quot;afterReturning ...&quot;); }</p>\n<p>// 异常通知（程序在出现异常的情况下，执行的后置通知） @AfterThrowing(&quot;pt()&quot;) public void afterThrowing(JoinPoint joinPoint){ log.info(&quot;afterThrowing ...&quot;); } }</p>\n<p>目标方法前的通知方法：字母排名靠前的先执行</p>\n<p>目标方法后的通知方法：字母排名靠前的后执行</p>\n<h4>如果我们想控制通知的执行顺序有两种方式：</h4>\n<p>修改切面类的类名（这种方式非常繁琐、而且不便管理）</p>\n<p>使用 Spring 提供的 @Order 注解</p>\n<h4>使用 @Order 注解，控制通知的执行顺序：</h4>\n<pre class=\"note-code\">@Slf4j @Component @Aspect @Order(2) // 切面类的执行顺序（前置通知：数字越小先执行 ; 后置通知：数字越小越后执行） public class MyAspect2 { // 前置通知 @Before(&quot;execution(* com.itheima.service.*.*(..))&quot;) public void before(){ log.info(&quot;MyAspect2 -&gt; before ...&quot;); }</pre>\n<p>// 后置通知 @After(&quot;execution(* com.itheima.service.*.*(..))&quot;) public void after(){ log.info(&quot;MyAspect2 -&gt; after ...&quot;); } }</p>\n<pre class=\"note-code\">@Slf4j @Component @Aspect @Order(3) // 切面类的执行顺序（前置通知：数字越小先执行 ; 后置通知：数字越小越后执行） public class MyAspect3 { // 前置通知 @Before(&quot;execution(* com.itheima.service.*.*(..))&quot;) public void before(){ log.info(&quot;MyAspect3 -&gt; before ...&quot;); }</pre>\n<pre class=\"note-code\">@Slf4j @Component @Aspect @Order(1) // 切面类的执行顺序（前置通知：数字越小先执行 ; 后置通知：数字越小越后执行） public class MyAspect4 { // 前置通知 @Before(&quot;execution(* com.itheima.service.*.*(..))&quot;) public void before(){ log.info(&quot;MyAspect4 -&gt; before ...&quot;); }</pre>\n<h4>通知的执行顺序大家主要知道两点即可：</h4>\n<p>不同的切面类当中，默认情况下通知的执行顺序是与切面类的类名字母排序是有关系的</p>\n<p>可以在切面类上面加上 @Order 注解，来控制不同的切面类通知的执行顺序</p>\n<h4>切入点表达式：描述切入点方法的一种表达式</h4>\n<p>作用：主要用来决定项目中的哪些方法需要加入通知</p>\n<h4>常见形式：</h4>\n<pre class=\"note-code\">execution(……) ：根据方法的签名来匹配</pre>\n<figure class=\"note-figure\"><img src=\"/notes-media/java-web-059.png\" alt=\"AOP 与事务管理 笔记配图\" loading=\"lazy\" /></figure>\n<pre class=\"note-code\">@annotation(……) ：根据注解匹配</pre>\n<figure class=\"note-figure\"><img src=\"/notes-media/java-web-060.png\" alt=\"AOP 与事务管理 笔记配图\" loading=\"lazy\" /></figure>\n<h4>execution</h4>\n<p>execution 主要根据方法的返回值、包名、类名、方法名、方法参数等信息来匹配，语法为：</p>\n<pre class=\"note-code\">execution( 访问修饰符 ? 返回值 包名 . 类名 .? 方法名 ( 方法参数 ) throws 异常 ? )</pre>\n<h4>其中带 ? 的表示可以省略的部分</h4>\n<p>访问修饰符：可省略（比如 : public 、 protected ）</p>\n<h4>包名 . 类名： 可省略</h4>\n<p>throws 异常：可省略（注意是方法上声明抛出的异常，不是实际抛出的异常）</p>\n<h4>示例：</h4>\n<pre class=\"note-code\">@Before(&quot;execution(void com.itheima.service.impl.DeptServiceImpl.delete(java.lang.Integer))&quot;)</pre>\n<h4>可以使用通配符描述切入点</h4>\n<p>：单个独立的任意符号，可以通配任意返回值、包名、类名、方法名、任意类型的一个参数，也可以通配包、类、方法名的一部分</p>\n<p>.. ：多个连续的任意符号，可以通配任意层级的包，或任意类型、任意个数的参数切入点表达式的语法规则：</p>\n<h4>返回值可以使用 * 号代替（任意返回值类型）</h4>\n<p>包名可以使用 * 号代替，代表任意包（一层包使用一个 * ）</p>\n<p>使用 .. 配置包名，标识此包以及此包下的所有子包</p>\n<h4>可以使用 * 配置参数，一个任意类型的参数</h4>\n<p>可以使用 .. 配置参数，任意个任意类型的参数</p>\n<h4>省略方法的修饰符号</h4>\n<pre class=\"note-code\">execution(void com.itheima.service.impl.DeptServiceImpl.delete(java.lang.Integer))</pre>\n<h4>使用 * 代替返回值类型</h4>\n<pre class=\"note-code\">execution(* com.itheima.service.impl.DeptServiceImpl.delete(java.lang.Integer))</pre>\n<h4>使用 * 代替包名（一层包使用一个 * ）</h4>\n<pre class=\"note-code\">execution(* com.itheima.*.*.DeptServiceImpl.delete(java.lang.Integer))</pre>\n<h4>使用 .. 省略包名</h4>\n<pre class=\"note-code\">execution(* com..DeptServiceImpl.delete(java.lang.Integer))</pre>\n<h4>使用 * 代替类名</h4>\n<pre class=\"note-code\">execution(* com..*.delete(java.lang.Integer))</pre>\n<h4>使用 * 代替方法名</h4>\n<pre class=\"note-code\">execution(* com..*.*(java.lang.Integer))</pre>\n<h4>使用 * 代替参数</h4>\n<h4>使用 .. 省略参数</h4>\n<pre class=\"note-code\">execution(* com..*.*(..))注意事项：</pre>\n<p>根据业务需要，可以使用 且（ &amp;&amp; ）、或（ || ）、非（ ! ） 来组合比较复杂的切入点表达式。</p>\n<pre class=\"note-code\">execution(* com.itheima.service.DeptService.list(..)) || execution(* com.itheima.service.DeptService.delete(..))</pre>\n<h4>切入点表达式的书写建议：</h4>\n<p>所有业务方法名在命名时尽量规范，方便切入点表达式快速匹配。如：查询类方法都是 find 开头，更新类方法都是 update 开头</p>\n<p>// 业务类 @Service public class DeptServiceImpl implements DeptService { public List&lt;Dept&gt; findAllDept() { // 省略代码 ... } public Dept findDeptById(Integer id) { // 省略代码 ... } public void updateDeptById(Integer id) { // 省略代码 ... } public void updateDeptByMoreCondition(Dept dept) { // 省略代码 ... } // 其他代码 ... }</p>\n<p>// 匹配 DeptServiceImpl 类中以 find 开头的方法</p>\n<p>描述切入点方法通常基于接口描述，而不是直接描述实现类，增强拓展性</p>\n<p>在满足业务需要的前提下，尽量缩小切入点的匹配范围。如：包名匹配尽量不使用 .. ，使用 * 匹配单个包</p>\n<h4>切入点表达式书写建议：</h4>\n<p>所有业务方法名在命名时尽量规范，方便切入点表达式快速匹配。如： findXxx ， updateXxx 。</p>\n<p>描述切入点方法通常基于接口描述，而不是直接描述实现类，增强拓展性。</p>\n<p>在满足业务需要的前提下，尽量缩小切入点的匹配范围。如：包名尽量不使用 .. ，使用 * 匹配单个包。</p>\n<pre class=\"note-code\">@annotation</pre>\n<h4>编写自定义注解</h4>\n<p>在业务类要做为连接点的方法上添加自定义注解</p>\n<h4>自定义注解 ： LogOperation</h4>\n<pre class=\"note-code\">@Target(ElementType.METHOD) @Retention(RetentionPolicy.RUNTIME) public @interface LogOperation{ }</pre>\n<pre class=\"note-code\">@Target 是原注解，修饰注解的注解。后面参数代表在方法上生效。 @Retention 也是原注解，代表这个注解什么时候生效。后面参数代表是在运行是生效。</pre>\n<h4>业务类 ： DeptServiceImpl</h4>\n<pre class=\"note-code\">@Slf4j @Service public class DeptServiceImpl implements DeptService { @Autowired private DeptMapper deptMapper;</pre>\n<pre class=\"note-code\">@Override @LogOperation // 自定义注解（表示：当前方法属于目标方法） public List&lt;Dept&gt; list() { List&lt;Dept&gt; deptList = deptMapper.list(); // 模拟异常 //int num = 10/0; return deptList; }</pre>\n<pre class=\"note-code\">@Override public void save(Dept dept) { dept.setCreateTime(LocalDateTime.now()); dept.setUpdateTime(LocalDateTime.now()); deptMapper.save(dept); }</pre>\n<pre class=\"note-code\">@Override public Dept getById(Integer id) { return deptMapper.getById(id); }</pre>\n<pre class=\"note-code\">@Override public void update(Dept dept) { dept.setUpdateTime(LocalDateTime.now()); deptMapper.update(dept); } }</pre>\n<h4>切面类</h4>\n<p>// 前置通知 @Before(&quot;@annotation(com.itheima.anno.LogOperation)&quot;) public void before(){ log.info(&quot;MyAspect6 -&gt; before ...&quot;); } // 后置通知 @After(&quot;@annotation(com.itheima.anno.LogOperation)&quot;) public void after(){ log.info(&quot;MyAspect6 -&gt; after ...&quot;); } }</p>\n<h4>当遇到增删改的方法名</h4>\n<p>yaml 、 yml 两种配置文件，优先级最高的是 yml 。</p>\n<p>配置文件优先级排名（从高到低）：properties 配置文件</p>\n<h4>yaml 配置文件</h4>\n<p>注意事项： 虽然 springboot 支持多种格式配置文件，但是在项目开发时，推荐统一使用一种格式的配置。（ yml 是主流）</p>\n<p>没有规律时，就可以使用 @annotation 切入点表达式</p>\n<h4>yaml 配置文件</h4>\n<p>命令行参数的优先级时最高的，同时配置的情况下，命令行参数的配置项生效。</p>"
      },
      {
        "id": "mvc-json",
        "title": "Spring MVC / JSON / 全局异常",
        "summary": "三层架构、请求注解、JSON 与 Nginx 反向代理。",
        "html": "<h4>3. JSON 对象</h4>\n<p>概念 ：用于数据传输的轻量级格式，实质是特定格式的字符串。</p>\n<p>语法规范 ：所有 key 必须加双引号 &quot;key&quot;: value 。</p>\n<h4>常用方法 ：</h4>\n<p>JSON.parse(str) ： JSON 字符串 → JS 对象。</p>\n<p>JSON.stringify(obj) ： JS 对象 → JSON 字符串。</p>\n<h4>四、 DOM 与事件监听</h4>\n<p>1. DOM ( 文档对象模型 )</p>\n<h4>Controller （控制层） ：接收请求、响应数据。</h4>\n<p>Service （业务逻辑层） ：负责具体的业务逻辑处理。</p>\n<p>DAO / Mapper （数据访问层） ：负责与数据库交互。</p>\n<pre class=\"note-code\">@Controller ：控制层。</pre>\n<pre class=\"note-code\">@Service ：业务层。</pre>\n<pre class=\"note-code\">@Repository ：数据访问层。</pre>\n<p>组件扫描 ：通过启动类上的 @SpringBootApplication 默认扫描当前包及其子包下的注解。</p>\n<p>3. DI 依赖注入注解与多实现类冲突解决</p>\n<pre class=\"note-code\">@Autowired ： Spring 框架提供，默认按照 类型 自动注入。</pre>\n<pre class=\"note-code\">@Resource ： JDK 提供，默认按 名称 注入。</pre>\n<pre class=\"note-code\">@RequestBody ：接收 JSON 格式的请求体数据。</pre>\n<pre class=\"note-code\">@PathVariable ：获取 URL 路径参数（如 /user/{id} ）。</pre>\n<pre class=\"note-code\">@RequestParam ：获取查询参数，可指定 defaultValue 设置默认值。</pre>\n<h4>2. Nginx 反向代理</h4>\n<p>网络架构 ：前端 → Nginx → Tomcat 。</p>\n<h4>主要作用 ：</h4>\n<p>1. 安全性 ：保护后台服务，不直接暴露 Tomcat 端口。</p>\n<p>2. 扩展性 ：后端增减服务器，前端无感知，只需在 Nginx 中调整。</p>\n<p>3. 负载均衡 ：将流量合理分发到多个后端节点。</p>\n<p>主键返回： @Options(useGeneratedKeys = true, keyProperty = &quot;id&quot;)</p>\n<p>由于稍后，我们在保存工作经历信息的时候，需要记录是哪位员工的工作经历。 所以，保存完员工信息之后，是需要获取到员工的 ID 的，那这里就需要通过</p>\n<p>Mybatis中提供的主键返回功能来获取。</p>\n<pre class=\"note-code\">&lt;foreach&gt; 标签，改标签的作用，是用来遍历循环，常见的属性说明：</pre>\n<h4>item ：集合遍历出来的元素 / 项</h4>\n<p>separator ：每一次遍历使用的分隔符</p>\n<h4>close ：遍历结束后拼接的片段</h4>\n<p>上述的属性，是可选的，并不是所有的都是必须的。 可以自己根据实际需求，来指定对应的属性。</p>\n<h4>JWT 是如何将原始的 JSON 格式数据，转变为字符串的呢？</h4>\n<p>其实在生成 JWT 令牌时，会对 JSON 格式的数据进行一次编码：进行 base64 编码</p>\n<p>Base64 ：是一种基于 64 个可打印的字符来表示二进制数据的编码方式。既然能编码，那也就意味着也能解码。所使用的 64 个字符分别是 A 到 Z 、 a 到 z 、 0- 9 ，一个加号，一个斜杠，</p>\n<p>加起来就是 64 个字符。任何数据经过 base64 编码之后，最终就会通过这 64 个字符来表示。当然还有一个符号，那就是等号。等号它是一个补位的符号</p>\n<p>需要注意的是 Base64 是编码方式，而不是加密方式。</p>\n<h4>你需要 Spring MVC 。</h4>\n<p>如果是比较传统的 Spring 项目，你可能需要自己考虑：</p>\n<p>spring-boot-starter-web ↓ ┌─────────────────────┐ │ Spring MVC │ │ Spring 相关依赖 │ │ JSON 相关 │ │ Web 相关依赖 │ │ 内嵌 Tomcat │ │ ...... │ └─────────────────────┘所以你不用自己一个个找。</p>"
      }
    ]
  },
  {
    "id": "linux",
    "title": "Linux",
    "subtitle": "系统入门 / 权限 / Vim / 进程",
    "summary": "Linux 文件系统、用户权限、Vim 与软件包管理等笔记，按模块阅读。",
    "topics": [
      {
        "id": "intro",
        "title": "Linux 简介与服务器",
        "summary": "物理机与云服务器，Linux「一切皆文件」。",
        "html": "<h4>Linux</h4>\n<p>物理服务器 只安装了 Linux 的操作系统的 pc 服务器</p>\n<h4>云服务器</h4>\n<p>Init 6 是重启系统</p>\n<h4>linux 中一切都是文件 包括内存磁盘 打印机 网卡 都用文件名来表示</h4>"
      },
      {
        "id": "path-fs",
        "title": "目录、路径与文件系统",
        "summary": "路径、目录操作、权限、压缩与磁盘查看。",
        "html": "<h4>pwd 查看当前目录</h4>\n<p>pwd 查看当前目录cd 进入某个目录</p>\n<h4>Clear 清屏</h4>\n<p>linux 的目录和文件</p>\n<h4>相对路径和绝对路径</h4>\n<p>全路径文件名包括了文件名和路径名 也成为绝对路径</p>\n<h4>从当前目录算起</h4>\n<p>如果当前目录是 /usr include/stdio.h 等同于 /usr/include/stdio.h 如果当前目录是 /usr/include stdio.h 等同于 /usr/include/stdio.h</p>\n<h4>操作目录和文件的时候才去绝对路径和相对路径都可以</h4>\n<p>linux 支持磁盘分区 叫做文件系统 文件系统挂载在文件目录</p>\n<figure class=\"note-figure\"><img src=\"/notes-media/linux-001.png\" alt=\"目录、路径与文件系统 笔记配图\" loading=\"lazy\" /></figure>\n<h4>不是自己创建的目录和文件不要乱动</h4>\n<p>/tmp 是临时目录 可以在里面创建文件和目录 但不保证她的安全</p>\n<h4>切换到某个用户之后不能够进入根目录 权限不够</h4>\n<h4>Init 0 是关机</h4>\n<pre class=\"note-code\">Mkdir 目录名 创建文件夹可以使用相对路径或者绝对路径</pre>\n<h4>(mkdir ccc</h4>\n<pre class=\"note-code\">Mkdir ccc/ddd)ls -l 路径 查看文件夹内容</pre>\n<h4>创建文件</h4>\n<p>Touch 文件名</p>\n<h4>屏幕中的命令 &gt; 文件</h4>\n<p>把内容写某个文件中，如果文件已存在将清空原有的文件内容</p>\n<h4>命令 &gt;&gt; 文件</h4>\n<p>把内容追加到文件中</p>\n<figure class=\"note-figure\"><img src=\"/notes-media/linux-002.png\" alt=\"目录、路径与文件系统 笔记配图\" loading=\"lazy\" /></figure>\n<p>比如 ls &gt; 1.txt 的结果如上图</p>\n<h4>rm -r 目录名字（可以多个目录）</h4>\n<p>Cp -r 旧目录或者文件名 新目录或者文件名</p>\n<h4>存放命令历史的文件是隐藏文件 用 ls 是现实不出来的</h4>\n<h4>目录和文件的权限</h4>\n<p>1 修改目录和文件的拥有者</p>\n<pre class=\"note-code\">Chmod -R 三维的八进制权限 目录文件列表</pre>\n<pre class=\"note-code\">Chmod -R 三维的八进制权限 目录文件列表-R 表示连同各子目录一起修改</pre>\n<figure class=\"note-figure\"><img src=\"/notes-media/linux-003.png\" alt=\"目录、路径与文件系统 笔记配图\" loading=\"lazy\" /></figure>\n<pre class=\"note-code\">Chmod -R whooperatorpermission 目标和文件列表</pre>\n<h4>u 文件的所有者</h4>\n<p>g 文件的所有者的组</p>\n<h4>ls -lt 可以显示最近被修改过的文件（最新的文件）</h4>\n<p>ls -lt|more 可以分页查看（按空格）</p>\n<figure class=\"note-figure\"><img src=\"/notes-media/linux-006.png\" alt=\"目录、路径与文件系统 笔记配图\" loading=\"lazy\" /></figure>\n<p>只能为文件创建硬链接 不能为目录创建硬链接硬链接不能创建文件系统</p>\n<h4>创建软连接 ln -s</h4>\n<p>软链接相当于为文件创建了一个快捷方式</p>\n<h4>上传和下载文件</h4>\n<p>在 windows 和 linux 之间进行传递文件</p>\n<figure class=\"note-figure\"><img src=\"/notes-media/linux-007.png\" alt=\"目录、路径与文件系统 笔记配图\" loading=\"lazy\" /></figure>\n<h4>打包和压缩</h4>\n<p>Zip -q -r -d -u 压缩包文件名 目录和文件名列表</p>\n<figure class=\"note-figure\"><img src=\"/notes-media/linux-008.png\" alt=\"目录、路径与文件系统 笔记配图\" loading=\"lazy\" /></figure>\n<h4>语法 find 查找目录 -name 文件名 -print</h4>\n<h4>如果是在当前目录就用 .</h4>\n<p>文件名一定要用 &quot;&quot; 来括起来示例</p>\n<figure class=\"note-figure\"><img src=\"/notes-media/linux-011.png\" alt=\"目录、路径与文件系统 笔记配图\" loading=\"lazy\" /></figure>\n<figure class=\"note-figure\"><img src=\"/notes-media/linux-012.png\" alt=\"目录、路径与文件系统 笔记配图\" loading=\"lazy\" /></figure>\n<h4>Mount on 叫做 挂载点</h4>\n<p>1k-block 表示总共的容量 以 k 为单位use% 如果超过 80 就要小心</p>\n<h4>df -m 以兆为单位显示</h4>\n<p>df -h 以 g 为单位显示磁盘空间</p>\n<h4>scp 远程拷贝目录和文件</h4>\n<p>如果想在 linux 之间传输文件可以使用 scp</p>\n<p>（也可以先把文件传进 windows 再进行传输）列出目录文件的树状结构</p>\n<h4>-d 只列出目录 不列出文件</h4>\n<p>-D 列出文件和目录的修改时间</p>\n<h4>-f 列出目录和文件名的路径</h4>\n<p>-f 列出目录和文件名的路径-a 列出隐藏的目录和文件统计目录大小 du</p>\n<h4>du -h 单位同上</h4>\n<p>获取帮助的命令 man</p>\n<h4>可执行程序的搜索目录</h4>\n<p>LD_LIBRARY_PATHCLASSPATH</p>\n<figure class=\"note-figure\"><img src=\"/notes-media/linux-015.png\" alt=\"目录、路径与文件系统 笔记配图\" loading=\"lazy\" /></figure>"
      },
      {
        "id": "user-perm",
        "title": "用户、组与权限",
        "summary": "用户组、rwx 权限、网络模式与 systemctl。",
        "html": "<h4>1 组管理</h4>\n<p>linux 的用户属于组 （角色）Groupadd 添加组名</p>\n<h4>Groupdel 删除组</h4>\n<p>组 存储在 /etc/group2 用户管理</p>\n<h4>创建用户</h4>\n<pre class=\"note-code\">Sudo useradd - -g 组名 -d 用户的主目录（默认为 /home/ 用户名） 用户名</pre>\n<h4>文件存放在 etc/passwd 文件中</h4>\n<pre class=\"note-code\">Sudo passwd 用户名 进行创建密码</pre>\n<p>密码存放在 /etc/shadow 中</p>\n<h4>命令历史 存放在用户主目录的 .bash_history 文件</h4>\n<h4>2 权限的标志位</h4>\n<h4>r 表示还有读取权限</h4>\n<p>w 表示有修改文件 同时需要有 r 权限 如果要删除文件需要有目录的 w 权限</p>\n<p>x 表示具有执行文件的权限 同时需要有 r 权限 文件是否能真的执行 还要有文件本身决定</p>\n<h4>w 表示可以在目录中新增删除移动 子目录和文件 同时需要具有 x 权限</h4>\n<h4>权限可以用八进制的数字表示</h4>\n<p>r 的值为 4 w 的值为 2 x 的值为 1Rwx=7R-X=5R--=4Rw-=6-wx=3--x=1---=0</p>\n<h4>o 表示其他用户</h4>\n<p>o 表示其他用户a 表示全部用户Operator+ 增加权限</p>\n<h4>= 重新设置权限</h4>\n<p>permission 权限</p>\n<h4>文件内容操作</h4>\n<p>cat 查看文件内容 适用于文本量很小的less 也可以查看</p>\n<figure class=\"note-figure\"><img src=\"/notes-media/linux-004.png\" alt=\"用户、组与权限 笔记配图\" loading=\"lazy\" /></figure>\n<h4>wc 可以统计文件的行数</h4>\n<p>wc* 可以统计文件夹中子文件的行数</p>\n<h4>查找文件内容 grep</h4>\n<p>grep &quot; 查找内容 &quot; 文件名列表</p>\n<figure class=\"note-figure\"><img src=\"/notes-media/linux-005.png\" alt=\"用户、组与权限 笔记配图\" loading=\"lazy\" /></figure>\n<p>head -n 数字 文件 没有数字的时候显示前 10 行</p>\n<h4>r 读权限 4</h4>\n<p>r 读权限 4w 写权限 2x 执行权 1</p>\n<h4>设置虚拟机的静态 ip</h4>\n<p>企业的网络有统一的规划和设计 服务器的网络由管理员确定</p>\n<p>办公家用电脑的 ip 地址由交换机 / 路由器动态分配</p>\n<h4>动态分配 ip 会改变</h4>\n<p>vmware 提供了 桥接模式 nat 模式 和 主机模式</p>\n<p>桥接模式 虚拟机和宿主机是平等地位 没有虚拟网卡 会给虚拟机分配真实的网络地址 网关 和 子网掩码等 可以与局域网中的主机相互访问 也可以访问互联网</p>\n<h4>nat 模式 网络地址转换</h4>\n<p>将使用 vmnet8 的虚拟网卡 有虚拟网卡 vmnet8 为虚拟机分配 ip 地址 网关 和 dns</p>\n<p>虚拟机可以和宿主机相互通信 可以访问局域网中的其他主机 也可以访问互联网 但是其他主机不能访问这个虚拟机</p>\n<h4>Host only 仅主机模式</h4>\n<p>使用 vmnet1 虚拟网卡 只能和宿主机相互通信 不能访问局域网中的其他的主机 也不能访问互联网</p>\n<h4>nat 模式配置简单 缺省使用 nat 模式</h4>\n<p>2 如果找不到虚拟网卡 就可以试试桥接模式 因为桥接模式不需要虚拟网卡</p>\n<p>3 吧虚拟机的网络参数改为静态 ip</p>\n<h4>网络连通性测试 ping</h4>\n<p>Ping 可以测试本机与目标主机是否联通 联通的速度 和 网络的稳定性</p>\n<p>Ping -c 5 -s 1024 目标主机的 ip 地址 （域名）</p>\n<p>-c 表示 ping 包的个数 windows 平台的选项是 -n</p>\n<p>-s 指定 ping 发送数据的字节数 默认是 84 字节 56 字节的数据 +28 字节的报头 最大不超过 65535 字节 windows 的选项是 -l</p>\n<p>hostname -I 可以显示所有的 ip 地址（这里是大写 i 不要认错了）</p>\n<figure class=\"note-figure\"><img src=\"/notes-media/linux-013.png\" alt=\"用户、组与权限 笔记配图\" loading=\"lazy\" /></figure>\n<h4>ping 网址可以得到域名</h4>\n<p>网络异常或者目标主机有问题的话 肯定不会返回信息</p>\n<h4>1 目标主机不存在或者没有运行</h4>\n<p>2 目标主机没有提供给指定端口的服务3 被防火墙拦截了</p>\n<h4>系统服务管理</h4>\n<p>重点关注的服务 防火墙 远程登陆 文件传输</p>\n<h4>Systemctl 操作 服务名</h4>\n<p>操作 有 stop start restart is-active 查看是否启动 status 查看状态</p>\n<p>enable 开机自启动 try-restart 之重启正在运行中的服务 disable 禁用开机自启动</p>\n<p>Systemctl list-units --type service -all 查看所有的服务状态</p>\n<p>Systemctl list-units-files|grep enabled 查看已经启动的服务</p>\n<p>Systemctl --failed 查看启动失败的服务列表</p>\n<p>在 ubuntu 中 firewalld 不是默认自带的 需要 install 一下</p>\n<h4>主机防火墙只有软件部分</h4>\n<p>环境变量 程序的执行需要参数 如果多个程序需要同一个参数 则应该把该参数设置为环境变量查看环境变量的命令</p>\n<h4>1 ， env</h4>\n<p>可以使用管道来查看 如果想要看指定的环境变量则可以使用 grep2 ， echo</p>\n<h4>常用的环境变量</h4>\n<p>1 ）， LANG2 ） SHELL</p>\n<figure class=\"note-figure\"><img src=\"/notes-media/linux-014.png\" alt=\"用户、组与权限 笔记配图\" loading=\"lazy\" /></figure>\n<h4>3 ） PATH</h4>"
      },
      {
        "id": "vim",
        "title": "Vim 编辑器",
        "summary": "Vi / Vim 命令模式与编辑模式常用操作。",
        "html": "<h4>vi 是最强大的文本编辑器</h4>\n<h4>命令模式和编辑模式</h4>\n<p>命令模式输入的是命令</p>\n<h4>如果想要再次输入 就按 （ i ）</h4>\n<p>K 上 j 下 h 左 l （不是 i ） 右</p>\n<h4>o 在光标所在位置下面插入空白行</h4>\n<p>0 在光标所在位置的上面插入空白行Ctrl+u 进行上翻操作</p>\n<h4>Ctrl+d 下翻半页</h4>\n<p>: n 光标跳到第 n 行</p>\n<h4>nG 光标跳到第 n 行的行首</h4>\n<p>Ctrl+g 显示光标所在位置行号和文件的总行数</p>\n<pre class=\"note-code\">Sudo vi /etc/passwd</pre>\n<h4>ls-a 才能看见 可以用 vi 打开</h4>\n<p>可以匹配所有字符 ？只能匹配一个字符</p>\n<p>Ls d* 表示以 d 开头的全部文件</p>\n<p>Ls *.h 表示以 .h 结尾的文件Ls d*.h 同理Ls ?.h 同理</p>"
      },
      {
        "id": "process",
        "title": "进程与系统状态",
        "summary": "ps / top / kill 与内存、交换区查看。",
        "html": "<pre class=\"note-code\">Ps -ef 可以查看系统正在进行的所有进程</pre>\n<pre class=\"note-code\">ps -ef | less 利用管道可以方便查看</pre>\n<h4>PID 是进程的编号</h4>\n<p>C 是百分比（占用总进程）</p>\n<h4>stime 表示总时间</h4>\n<p>time 是使用 cpu 的时间</p>\n<h4>cmd 是命令</h4>\n<p>如果想要查看我们想要关心的进程 可以用管道来过滤（ grep ）</p>\n<p>grep --color=auto test 是 grep 命令产生的进程</p>\n<pre class=\"note-code\">ps aux|less</pre>\n<p>less 命令可以按 q 推出</p>\n<pre class=\"note-code\">Kill 程序编号</pre>\n<p>killall 程序名字 都可以终止程序进行</p>\n<h4>top 性能分析</h4>\n<p>top 能实时显示系统运行的总体状态和每个进程使用资源的情况</p>\n<h4>top [ 选项 ]</h4>\n<p>Load average 表示进程负载 表示在一段时间内正在进行和等待进行的进程之和 users 表示登录的用户数量</p>\n<p>Total 表示总共的进程数量 running 在正在运行 stop 表示停止 zombie 表示僵尸进程</p>\n<h4>cpu （ s ）表示进程所占的百分比</h4>\n<p>Us 用户进程百分比 sy （内核进程）操作系统的进程 ni 改变用户优先级的进程</p>\n<h4>id 表示空闲应用的百分比</h4>\n<p>如果 us 进程需要和系统打交道 如读写文件等等 就需要使用内核进程</p>\n<h4>Mib mem 表示内存的统计</h4>\n<p>total 表示系统有多少内存 free 空闲 used 使用中</p>\n<p>Mib swap 交换区（虚拟内存） 如果服务器的内存很低就会使用交换区（在硬盘中 读写速度比内存要低很多）重点关注以下内容</p>\n<figure class=\"note-figure\"><img src=\"/notes-media/linux-009.png\" alt=\"进程与系统状态 笔记配图\" loading=\"lazy\" /></figure>\n<figure class=\"note-figure\"><img src=\"/notes-media/linux-010.png\" alt=\"进程与系统状态 笔记配图\" loading=\"lazy\" /></figure>\n<h4>使用网络和网络编程</h4>\n<p>只需要具备计算机网络的基本常识</p>\n<h4>free -m 显示交换区和内存</h4>\n<p>free -h 以 g 为单位来显示</p>"
      }
    ]
  }
]
